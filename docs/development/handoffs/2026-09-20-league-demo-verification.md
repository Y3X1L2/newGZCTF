# 双港 Demo 目标验证方案核查

## 本次目标与决定

用户认可“双港调度争夺”故事 Demo，询问是否仍需要 Flag、Checker 验证后发 Flag 是否适用，并要求区分当前必须规划实现和开发中可以继续商讨的内容。

用户随后明确赛制尚未确定，可以不同于旧文档。新方案因此把“已验证目标”与“阶段得分、优势、KO 和终局”分开；旧的 60 秒、6 次、产币和金币判胜仅作参考。当前没有将新的胜负参数写成已确认规则。

## Git 和核查范围

- 延续 `codex/league-roadmap-20260919`，worktree 为 `D:/Work/newGZCTF-league-roadmap-20260919`，起始提交 `e43c4030`。
- 已执行状态、fetch、日志及远端 main 引用检查，main 仍为 `f389d26c548802d6caabd974efd3053d4dcbd0bd`。
- 核查 FlagChecker、AWDP Flag 提交、AwdpCheckerService、AwdpScriptRunner、ExerciseService、TeamLabProtocolEventService 和 ApiOperationService。
- 未执行脚本、攻击、部署、迁移或业务代码修改。

## 主要结论

1. Flag 可以作为阶段材料或展示凭证。主线推荐由平台可信执行端验证实际业务效果，保存归属明确的结果；回执显示和兼容提交使用同一事实记录。
2. 选手只申请验证，平台选择已审核脚本、目标和参数。Checker 校验选手取得的能力，裁判自己的读取权限用于核对，不替选手创造成功效果。
3. 健康、目标和持续挑战使用不同语义；持续时间及次数按目标设计确定。
4. 当前 AWDP 执行器未配置脚本时返回 OK，默认超时 30 秒，直接启动主站系统进程。联赛不能据此声称已有可信目标验证，需要配置检查、隔离执行、明确截止及结构化结果。
5. 当前协议事件接收只表示记录了事件，不能替代目标验证。
6. 新增的是裁判业务尝试、任务和证据状态；实例生命周期继续走已有统一队列。

## 产物与后续

新增[Demo 验证设计与实施清单](../league/demo-verification-20260920.md)。旧路线和并行规范顶部增加场景更新说明，保留历史方案供追溯。9 月 19 日的 Word 是当日版本，本次没有重新生成它。

新文档继续按“说人话”技能编写，给出 Flag/Checker 推荐方式、验证流程、当前代码差距、底座决定、首个验证 Demo 必做内容及开发中可调整项。未把推荐回执设计或新的胜负条件写成已经开发的功能。

已通过 17 项源码断言、61 个相对链接及 UTF-8 检查；文档差异检查通过，暂存后再执行 `git diff --cached --check`。本次不运行前后端全量测试。最终提交以本记录所在提交为准，推送后核对远端引用；不合并 main，不改变生产状态。

用户副本为 `C:/Users/Cloud/Downloads/双港Demo验证设计与实施清单-20260920.md`。其中源码与其他文档链接使用仓库网页地址，便于单独阅读；正文与仓库版本一致。

后续由 lxy 与出题人先选定一个可复现的业务目标，出题人交具体逻辑与样例，yhr 接入可信执行，lmr 实现尝试和判定，lcx 展示过程与结果。完整胜负规则、预算数值和连续参数在适用功能编码前确认。
