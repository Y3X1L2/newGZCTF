# 联赛第一阶段缩减方案交接

## 任务目标

根据 lxy 与负责人最新商讨结果，第一阶段完成进入、报名、配置、启动、核心 Flag KO 和基础金币加减。商店建议后置，必要时仅增加测试扣费商品。9 月 30 日左右跑通的目标继续沿用。

本次交付规划、分工、并行合并安排与同版 Markdown/Word。联赛业务实现属于后续任务。

## 基线

- 延续同一规划任务分支：`codex/league-roadmap-20260919`。
- Worktree：`D:/Work/newGZCTF-league-roadmap-20260919`。
- 本次起始提交：`c42be8f00416b8fa793f7270e289839c9c7d5491`。
- 重新执行 fetch，远端 main 仍为 `f389d26c548802d6caabd974efd3053d4dcbd0bd`，未前进。
- 主工作区保持原分支；本次未连接测试或生产服务器。

## 当前状态

规划交付 complete，文档验证 VERIFIED；联赛业务实现与运行验收 NOT_RUN。

lxy 已明确同意三项配套规则：管理员审核两队并手动开赛；第一笔在服务端校验成功并写入赛果的 Flag 提交获胜；首期使用约定的业务账号与防守配置，整机最高权限在后续阶段加入。

首期将原 Checker 自动目标判定、服务产币、金币判胜和完整商城安排移至后续。旧文档顶部与导航已标明取代关系，旧 Word 保留为 9 月 19 日快照。

## 技术结论

- 当前 GameController 有报名和审核状态逻辑；接入时优先复用报名事实，通过公开 Application 契约增加双队选择与名单冻结。
- PenetrationTeamLabAdapter 有按队运行管理能力。玩家访问与跨队攻击网络分别验收，准备期间保持选手访问关闭。
- PenetrationObjectiveService 当前校验本队环境 Flag；新联赛校验本场对方核心，绑定运行代次，并将有效提交与 KO 终局置于同一本地事务。
- lxy 拥有场次与终局写入，lmr 拥有 Flag 判定和账本，yhr 拥有运行适配及注入，lcx 拥有前端。出题人提供场景及操作范围。
- 数据库保证终局唯一，金币有原子加减和流水。比赛结果、关闭访问及资源清理分别记录，清理意图持久化恢复。

## 改动范围

- 新增 [第一阶段最小实现与分工](../league/phase1-minimal-20260920.md)及同版六页 Word。
- 更新 docs/README、原路线、并行规范和 Demo 验证文档的适用范围。
- 文档生成器增加来源、输出、标题和日期参数，保留原默认调用方式。
- 业务源码、数据库迁移、运行 API 和生产环境均未修改。

## 验证证据

| 验证项 | 结果 |
| --- | --- |
| Git 格式检查 | git diff --check 通过 |
| 文本和链接 | 73 个相对文件链接存在，UTF-8 无替换字符 |
| 内容一致性 | Markdown、DOCX 正文和 Word 渲染文本按空白规范化后完全一致 |
| 字体 | OOXML 清除标题主题字体；PDF 中全部汉字实际为 SimSun 或 SimHei |
| 排版 | Microsoft Word 导出后逐页查看 6 张 PNG，无孤立尾页、缺字、遮挡或截断 |
| 生成器 | 参数化命令成功生成本次 Word |
| 业务测试 | 未执行后端 build、单元/集成、前端全量和真实比赛验收；本次没有业务代码变化 |

标准 render_docx.py 因运行包缺少 LibreOffice soffice.exe 失败，改用本机 Microsoft Word COM 导出 PDF，再由 bundled pypdfium2 渲染。WPS 未自动打开实测。字体检查基于实际 PDF 字体，未仅依赖样式名称。

QA 结果保存在仓库外 `D:/Work/league-minimal-qa-20260920/word-v2`，未提交 PDF 或 PNG。

## 提交与部署

- 提交身份：本记录所在的 `docs: simplify league phase one to flag KO` 提交；交付时报告实际 SHA。
- 推送分支：`codex/league-roadmap-20260919`，保留此前规划历史。
- 尚未合并 main；分支和 worktree 保留供审查。
- 没有测试或生产部署，没有数据库变更。

## 后续工作

下一步由 lxy 组织最小契约 PR，明确新模块依赖和现有报名接入。yhr 与出题人交付固定场景及账号权限清单，lmr 与 yhr 固定 Flag 生成和注入约定。开发使用本文更新后的任务范围和原通用并行规范。

已有 CI 失败仍以此前 f389d26c 运行记录为据，本次没有重新运行或修复。开工阶段复验并分配处理；真实双队联赛能力需完成源码、测试和目标环境验收后签收。
