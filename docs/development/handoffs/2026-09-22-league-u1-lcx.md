# 联赛 U1 lcx 实施检查点

本轮按 V1.0 手册实施 T0 会审与 U1。用户要求暂缓报名名单缺口修复，继续其他 U1 功能；不修改 lxy 后端，不部署。

## 基线与依赖

分支 `codex/league-u1-lcx` 从最新 `origin/main@9999e6b1` 创建，独立 worktree 为 `newGZCTF-league-u1-lcx`，随后快进合入 lxy 契约候选 `2ec37d8c`。lxy 提交尚未在 main 中，后续 PR 必须说明这一依赖，待其合入后同步主线。

## 已完成

U1 feature API adapter 封装真实生成客户端的列表、详情、创建、修改、报名、审核和双队席位接口。保留调用者 revision、席位顺序、服务端错误与响应，不自动重试写操作。未手改生成代码。

本机定向测试 4/4；TypeScript strict、lint（0 warning/error）、前端架构检查通过。未运行完整前端 build/全量测试、后端测试、真实 HTTP 或浏览器验收。此检查点不代表 U1 完成。

## 后续实施与验收

1. 增加 vNext 列表/详情路由与 controller：身份隔离缓存、游标分页、登录/禁用/错误状态。验证刷新与游标恢复。
2. 增加创建与配置表单，复用真实 TeamLab 场景和 release 目录；管理员审核、双队选择，队长报名。冲突保留编辑内容并提示刷新，不自动覆盖。暂不展示名单，不修复后端名单问题。
3. 添加页面交互测试；运行完整前端门禁；检查 390/1366/1920/2560、日夜、键盘与减少动效。生产入口保持关闭，不能注入样例冒充真实成功。
4. 联调环境/测试身份尚未知；真实 U1 保存回读待补。U2–U4 不在本轮实现范围。

本次因项目规定单任务 4,000-token 预算建立检查点，尚未增加页面或导航。下一轮从以上状态继续，不重复初始化分支。
