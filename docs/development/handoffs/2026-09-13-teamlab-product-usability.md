# TeamLab 产品化改造记录

本文记录 2026-09-13 开始的 TeamLab 使用闭环改造。范围以用户批准的改造计划为准，不包含批量操作、场景复制、协议解析、运行版本切换、抓包扩建、全资源对账、PVE 和快照。

## 实施清单

- [x] 链路策略覆盖网段全部资产，失败时恢复本次已修改目标，现场成功后再标记生效
- [x] 链路策略使用普通字段，NAT 移入资产服务开放
- [x] 资产服务开放形成公网入口创建、查看、撤销和销毁清理闭环
- [x] 容器与 VM 文件管理补齐目录、移动、重命名和递归删除
- [x] 运行状态、失败动作、取消创建和运行状态检查改为用户可理解的表达
- [x] 集中会话支持关键词、协议和异常筛选，显示用户、资产和节点名称
- [x] 设备包前端改称设备模板，登记参数改为结构化表单
- [x] 连接器支持读取 Worker 网卡后选择，同时保留手工填写
- [x] 协议事件接口补齐必填与长度校验
- [x] 定向测试、前后端 Release 构建与 `git diff --check`

## 验证记录

- 后端相关定向测试：76/76 通过，覆盖链路策略、服务开放、文件运维、连接器、协议事件和远程会话。
- 前端相关定向测试：16/16 通过。完整测试首次运行 319/327 通过，其余 8 项均为新增字段或新文案后的旧测试夹具；修正后对应 3 个测试文件 14/14 通过，没有重复执行整套测试。
- `pnpm check`、locale 校验、vNext lint 和前端架构检查通过。
- `dotnet build src/GZCTF.slnx -c Release --no-restore`：0 警告、0 错误。
- Vite 生产构建、制品清单和 bundle budget 检查通过。
- `git diff --check` 通过。

本轮没有连接或修改服务器，也没有提交、推送或部署。公网服务地址、VM SFTP、容器文件通道和现场网卡仍需在获得部署指令后做真实基础设施验收；本记录不以单元测试代替该项结论。
