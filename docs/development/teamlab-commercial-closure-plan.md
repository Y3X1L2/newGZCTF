# TeamLab 商业能力补齐与验收清单

基线：2026-09-08，已快进同步至 origin/main `3a70126`，保留全部本地在研修改。
分支：`codex/teamlab-commercial-closure`。
依据：`teamlab-ankelu-gap-audit-20260907.md` 第 3 至 9 节。

## 范围和验收原则

2026-09-08 最新范围确认：用户明确排除 A06 快照/回滚，其余能力继续实施。补充审查工控协议模拟，优先核对已有 Modbus 报告对应的可构建制品、真实寄存器/线圈语义、参数消费及协议事件链；历史报告不代表当前环境已通过。H01 仍未完成，登记和租约不等于 provider 接入。

### 当前轮次：R05、A01、A04、A05

用户要求本轮集中完成这四项，不以其他编号的零散修复替代。四项前后端实现已接入；A01、R05、A05 的真实本地 API 链路通过，A04 容器和 OpenSSH 协议实测通过。硬件 KVM、真实 VM 管理网络上的 SFTP 和有数据浏览器视觉验收仍须单列，不能把本地协议验收写成生产签收。

- R05：用户已确认仅保存会话操作审计证据，不录制命令、终端输出或屏幕。实现实际 JSON 文件、会话归属授权、完整性校验、下载、保留期、配额及过期删除。新增 ReadyAt 前向迁移，以“登记/配额保留 -> 对象写入 -> 就绪”支持中断恢复。基础、中断恢复和 PostgreSQL/真实文件测试均已通过；有数据浏览器下载验收待补。
- A01：六种单资产操作已接入原 DeploymentQueueTicket。操作使用 LifecycleManage 权限；原节点保存在受保护负载中，避免误用主站协调任务的空 TargetNodeId。停止/启动与清理/创建分别保存阶段，失败可按 Retryable 继续未完成步骤。当前代次、节点、原生身份和不可变计划在执行前复查；停止/暂停意图持久化，对账不再将其误报为资源缺失。只结束目标资产会话，文件传输与生命周期通过既有分布式租约协调。前端有权限/能力提示、原因、危险确认、真实状态和可在刷新后恢复的任务入口。真实 Docker 主站到队列、Agent 的五个任务与对照资产隔离通过，重启的阶段恢复测试通过；跨节点 OVN、真实 KVM 重建和来宾业务健康仍未签收。
- A04：容器文件与 VM SFTP 统一接入资产文件面板，单文件 8 MiB、单目录 1000 项。容器 openat2 解析拒绝宿主挂载、符号链接和特殊文件；VM 先验证 libvirt UUID/代次和地址，再通过管理网络使用镜像 SSH 账号。首次探测登记主机身份后才执行文件操作；身份变化拒绝连接，并提供经 LifecycleManage 授权、确认后重新登记的入口。覆盖保留已有普通文件权限，原子替换；删除仅文件/空目录。保留目录中的超时上传残留在下次目录读取或上传时回收，近期暂存不会被误删。Docker 和真实 OpenSSH 的上传、下载、覆盖、删除、身份变化与残留回收已做专项验证；完整 VM 管理网络和浏览器文件选择/下载交互待现场验收。
- A05：libvirt 图形控制台不依赖 VM 内网络，含 loopback VNC graphics、UUID/代次校验、实际 RFB 探测、中继和 Guacamole 临时用户/连接。诊断遵守配置中的 libvirt URI。隔离的真实 libvirt/QEMU TCG 域没有来宾网卡，已通过主站创建、Guacamole WebSocket 接收图形数据、关闭和连接清零；不是硬件 KVM 验收。Guacamole HTTP 日志已移除含令牌的 URL 记录。

前端入口为 `http://127.0.0.1:5173/`。浏览器控制工具连续报 `failed to write kernel assets: 系统找不到指定的路径 (os error 3)`，有数据的多尺寸/主题/键盘视觉验收未完成。真实 API 测试使用正常登录的临时管理员，不读取或重置原管理员密码；新增 API 验收账号结束后均已禁用，保留审计身份。未修改生产或公网入口。

当前代码门禁及最终结果见本节末尾的最终验证记录。历史的 1071/307/280 计数不再代表本轮最终状态。SFTP 依赖升级为 SSH.NET 2026.0.0 和其要求的 BouncyCastle 2.7.0；真实 OpenSSH 测试使用不含运行密码/密钥、可缓存的专用镜像，避免每次容器启动都下载软件。

本轮新增文件接口后的全量集成回归已完成：280/280，退出码 0，测试运行报告约 4 分钟。新版主站 OpenAPI 已包含文件接口，匿名调用返回 401，生成客户端更新后的严格 TypeScript 检查通过；真实 Docker 终端、诊断和文件脚本均通过。后续 Agent 文件覆盖权限保留增量按真实 Docker 专项验收，不将此前全量门禁扩大解释为包含该后续改动。

恢复后的验证与下一步：

1. Docker Desktop/WSL 及 `gzctf-agent-local-sim`、`newgzctf-main-db-1`、`newgzctf-main-redis-1`、`newgzctf-main-guacd-1` 已恢复，不删除卷。
2. `TeamLabRemoteAuditPersistenceTests` 2/2 通过；本地已前向升级 ReadyAt 与 GuacamoleCreationStarted 两项迁移。后者在 Guacamole 创建前持久化阶段，对历史活跃 VM 会话保守回填，避免探测失败时误等不存在的 Guacamole 资源，也不丢失旧会话清理责任。
3. 主站/Agent 制品及外部 OpenAPI/内部客户端已更新，兼容检查通过。主站运行于独立临时目录 `C:/Users/87701/AppData/Local/Temp/teamlab-main-console-phase-ee35ad0cb30641ecb300eacaf35ce079`，内容根与原本地配置保持不变。两份本地迁移前备份位于 `C:/Users/87701/AppData/Local/Temp/teamlab-before-r05-20260907.dump`（2303021 字节）与 `C:/Users/87701/AppData/Local/Temp/teamlab-before-console-phase-20260907.dump`（2484877 字节），不提交仓库。Docker cp 导出异常，改为二进制流提取原备份；不是迁移后重新备份替代旧备份。
4. 真实 libvirt/Guacamole 的创建、图形连接和关闭已在 TCG 完成；继续补硬件 KVM、真实 VM 管理网络 SFTP、OVN 网络重接和有数据浏览器验收。

上一轮恢复记录（制品已继续更新）：旧 Agent 容器 `gzctf-agent-local-sim-before-files-20260907` 停止保留，数据库卷未删除。R05 默认保留 90 天、存储上限 256 MiB，通过 `TeamLab:RemoteAudit:RetentionDays` 和 `TeamLab:RemoteAudit:MaxStorageBytes` 配置；对象存储必须为私有存储，不直接公开其路径。所有迁移只应用于本地，未生产部署。

### 2026-09-08 实际验收与运行数据

- 正常账号登录后，通过真实 HTTP 执行上传/下载、暂停、恢复、停止、启动、重建，五个原队列票据 Succeeded；停启保留文件，重建清空目标资产可写层，对照资产未替换。
- 真实容器终端 WebSocket 创建、交互和关闭后，归档操作证据并下载校验摘要；`contentRecorded=false`。验收脚本未将终端输出或 VNC 图像写入证据文件。
- 真实 libvirt/TCG 磁盘为空的 BIOS 域通过 VNC 中继连接 Guacamole，实际收到图形流；关闭后查询 Guacamole 临时连接数为 0。此域没有网卡、不代表平台创建的硬件 KVM 业务镜像。
- 本地验收 runtime：`01a07cc8-a47f-7700-8255-a0fb3f7c60fe`，topology：`01a07cc8-a355-7696-818d-2e619377d332`。其中容器按不可变 Redis 镜像真实创建，TCG 域仅为控制台导入验收对象，没有伪造成功结果。
- 新迁移前备份：`C:/Users/87701/AppData/Local/Temp/teamlab-before-power-sftp-20260908-export.dump`，3270159 字节，SHA-256 `AC0BFE0419636BFFD09279DE7B3BFCD0846D9DB9C9716121396F5E857C35B9F9`。Docker cp 导出异常后，以二进制流导出同一份迁移前备份。
- 未提交、未推送。独立验收服务和 fixture 数据保留用于浏览器工具恢复后的复核；临时 API 用户已禁用，审计记录保留。清理这些对象时必须先结束会话，再按 fixture 的 runtime/节点/容器标识处理，不能清空整个开发库。

### 最终验证记录（2026-09-08）

- Release 构建通过；后端全量单测 1086/1086。前端 locales、lint、严格 TypeScript、架构、312/312 测试、生产构建与体积门禁通过；生成客户端更新后的严格类型检查通过。
- 全量集成 284/284 通过。之后文件错误状态码/允许覆盖时创建缺失文件的增量执行了 16 项定向测试和最终真实 API/Agent 流程，未再扩大为全量集成重跑。之前的 SFTP 运行时下载 DNS 失败已通过可缓存、按 Dockerfile 摘要命名的测试依赖镜像处理，不作为业务成功证据。
- OpenAPI 已生成并通过兼容比较；`git diff --check` 通过。主站、队列、真实 Docker、终端、证据下载、libvirt/TCG、Guacamole WebSocket 图形数据及关闭清理最终实测通过。主站可辨认文件不存在（404）、权限不足（403）与节点不可用（503），不再把这些情况统一报 500。
- 当前主站：`C:/Users/87701/AppData/Local/Temp/teamlab-main-errors-20260908`，PID 31480；内容根仍为仓库 `src/GZCTF`，入口 8080/5173。当前主模拟 Agent：`teamlab-agent-errors-20260908` 临时制品，容器 `gzctf-agent-local-sim`，5001 仅本机绑定。TCG 验收 Agent 运行 `teamlab-agent-final-20260908`（VNC 代码与最终 Agent 相同），节点端口 5003。
- 最后复核：数据库迁移头 `20260907162107_TeamLabSftpHostIdentity`；活跃远程会话为 0，Guacamole 临时连接为 0。保留两只实际验收容器、磁盘为空的 TCG BIOS 域及专用 Guacamole 服务用于复核；没有删除原开发数据库卷。
- 剩余验收：浏览器工具恢复后的多尺寸/主题/键盘与下载交互；硬件 KVM 和真实 VM 管理网络上的 SFTP；跨节点 OVN 网络重接和业务健康。V1、无快照的导入域不支持单资产生命周期，界面明确提示限制；不会伪造重建结果。

保留声明式拓扑、不可变发布、统一 DeploymentQueueTicket、rollout、权限、WireGuard/OVN、链路策略与 Webhook。排除 PVE 管理和硬件透传管理面。
每项包含真实执行、权限与资源归属校验、幂等及代次保护、进度与失败反馈、恢复清理、前端操作及 API 回读。自动化、本地真实容器、Linux 多节点与物理端点验收分别记录，不互相替代。

## 产品能力地图

### A07/O01/H01–H03 本轮闭环约束（2026-09-08）

H03 追加工作（在研，未签收）：用户要求补齐软件缺口，并允许模拟缺失的物理测试条件。采用独立 Docker 测试夹具提供真实 OVN/OVS 数据面、QEMU Linux 来宾和独立 network namespace 外部端点；外部端点只作为物理接入模拟，不宣称真实硬件通过。先验证网络 provider 的互通、隔离、抓包、断链恢复及精确清理，再接入主站统一队列和 H01 provider 生命周期。现有 BIOS 域和无网段 Redis fixture 不计作 H03 证据。

正在修复的执行缺口：重复 apply 不能仅凭计算资源 running 就返回已应用，必须复查 OVN 意图及 OVS 端口；VM-only 分片 inventory 不应调用未配置的 Docker daemon。后续仍需补主站父状态恢复、连接器真实接入和全链路验收，不将 provider 专项等同完整产品验收。

本轮已获授权补齐以下能力；此处是实施计划，不是完成声明。

- A07：在资产访问区管理端口入口，复用现有端口分配和节点转发能力；创建、实际探测、撤销、重启恢复和销毁清理使用同一资源事实。
- O01：运行详情先只读检查 DB 与节点事实，展示差异和明确处理动作；修复复用现有资产/运行任务，保留主动停止意图，节点失联不判定资源缺失。
- H01：连接器的目录与租约接入真实 provider 生命周期，首个 provider 必须支持准备、接入、探测、解绑及 inventory；未支持的接入方式明确禁用，不将占用记录作为接通证据。
- H02：设备包沿用 Docker/VM 资产执行链，将发布时制品摘要、参数与健康要求贯穿计划、执行、诊断和销毁；不新增设备包专属运行队列或插件平台。
- H03：提供可重复的混合环境验收流程，覆盖互通、隔离、观测、故障恢复和精确清理；本地模拟与物理设备实测分别记录，未具备的实机条件不能标记通过。

产品入口保持在现有运行详情和资源管理中；新增状态必须对应真实服务端事实。各切片完成后同步实现、测试和剩余边界，不通过新增空壳面板充数。

| 编号 | 能力与用户闭环 | 验收重点 | 状态 |
| --- | --- | --- | --- |
| R01 | 容器终端：创建、交互、调整尺寸、中断、关闭 | xterm.js，二进制 UTF-8，PTY，方向键/Tab/Ctrl-C/粘贴，真实 shell | 本地真实 Docker 已通过 UTF-8、PTY、resize、Ctrl-C、inventory、取消和 shell 进程回收；浏览器完整交互待验收 |
| R02 | 会话创建取消、断线重新创建、弹窗失败补偿 | 异步取消不泄漏，服务端返回真实结束状态 | 已实现；StrictMode、取消、离开页面、清理重试 4 项回归通过，VM 弹窗实测待补 |
| R03 | Guacamole 清理与 Agent 重启对账 | 清理待重试可见，资源不存在可幂等成功，失联不显示连接成功 | 已接入清理和 inventory 对账，待专项回归 |
| R04 | 集中会话列表、筛选、强制结束、清理重试 | 用户/runtime/节点/状态/期限，授权与审计 | 已实现，待专项回归 |
| R05 | 远程审计文件查询、下载、保留和清理 | 真实文件、摘要、访问授权、配额和过期删除 | 非录制型证据文件及前后端已实现；PostgreSQL/实际文件归档、迁移恢复、下载与到期删除通过，本地已升级；登录后浏览器验收待补 |
| A01 | 单资产启动、停止、重启、重建、暂停、恢复 | 统一 ticket，代次保护，失败重试，危险操作确认 | 前后端已实现；真实主站/队列/Docker 及阶段恢复通过；KVM/OVN 现场验收待补 |
| A02 | runtime 暂停/恢复及批量选择进度 | 复用生命周期与 rollout，逐项真实结果 | 管理端、Open 和 rollout 已接统一 ticket；operation 等待票据结果，rollout 归属入队/执行双重检查；批量 UI 与真实端到端验收待补 |
| A03 | 单资产 inspect、日志工作台 | 受限输出、日志有界、刷新/错误/空态 | Docker 状态/日志及 VM 电源状态 API/面板已实现；VM UUID/代次校验与审计已接入，真实 KVM、旧 VM 身份恢复、来宾诊断及浏览器完整链路待补 |
| A04 | 容器文件管理及 VM SFTP | 目录/上传/下载/删除，路径约束，链接逃逸防护，审计 | 前后端已实现；真实 Docker/OpenSSH 协议通过；VM 管理网络和浏览器验收待补 |
| A05 | VNC 远程访问 | 能力探测、连接、租约、断线、清理 | 真实 libvirt/TCG → Agent → Guacamole 图形数据和关闭清理通过；硬件 KVM 与浏览器视觉验收待补 |
| A06 | 资产和场景快照、回滚、删除 | 持久化元数据，真实 Docker/VM 执行，一致性和失败补偿 | 待实现 |
| A07 | 受审计端口映射 | 分配、冲突检查、应用、实际可达、撤销与重启恢复 | 修复 NAT 节点/路由器归属、端口级执行和原代清理；真实 OVN NB 应用/撤销/精确清理通过；入口分配、冲突协调、可达探测和完整产品入口仍待补 |
| O01 | 手工对账与差异预览 | DB/Agent inventory，显式修复命令，结果事件 | 资产差异识别、确认修复和原队列完成已通过真实主站/Edge；网络/连接器差异、原代残留清理，以及资产恢复后的运行/分片失败状态恢复仍待补 |
| O02 | 任务列表、取消、重试资格、阻塞原因 | 统一队列、运行中协作取消与清理、重启恢复 | 已修复取消假成功；运行详情接入票据阶段、失败分类、重试资格和历史分页；运行中协作取消及完整浏览器验收待补 |
| O03 | 全局 runtime 与资产组合检索 | release/创建人/节点/资产/错误/状态，稳定分页 | API 与独立检索视图已实现；管理员全局、非管理员本人范围，当前代次过滤及 PostgreSQL 分页实测通过；登录后浏览器多尺寸验收待补 |
| O04 | 抓包历史、多任务、分段、下载和清理状态 | 不再只管理 captures[0]，每节点进度及失败原因 | 已接入历史分页和分段详情，定向测试通过 |
| O05 | Docker/KVM 独立心跳与能力降级 | 无 KVM 不阻断 Docker，取消不吞掉 | 已实现，定向测试通过；待实际 Agent 验证 |
| I01 | Open 远程会话连接协议 | token 身份/scope/grant，真正可消费终端与连接入口 | 创建/关闭已复用持久 operation；connect/terminal 与逐会话授权已接入，外部 DTO/生成契约已补；真实 Token 全链路待验收 |
| H01 | Connector provider 执行与生命周期 | prepare/attach/probe/detach/inventory，至少一种真实 provider | 专用网卡 provider、计划编译、节点约束和登记表单已接入；真实数据面专项通过，主站完整生命周期、清理/重启对账及状态回读尚待闭环 |
| H02 | 设备包受信制品与执行监督 | 摘要、参数、权限、健康、日志、事件、停止清理 | 接入制品/资源/参数校验、Docker 参数环境和 VM fw_cfg、声明的启动健康检查；修复前端绑定/时间契约并提供基本参数表单；持续健康监督、协议事件及真实设备程序消费验收仍待补 |
| H03 | 虚实混合恢复与物理验收 | Docker/VM/物理端点互通隔离、抓包、重启、销毁对账 | 本地真实 OVN/OVS＋QEMU Linux＋模拟外部端点 provider 专项 1/1 通过；主站队列/H01 生命周期、平台抓包及真实物理验收未完成，详见 2026-09-08 H03 验收记录 |

## 验证与交接

### 剩余工作优先级

1. 可靠性修复：R03 节点失联反馈、清理失败退避、Agent 重启后终端执行身份恢复、主站重启后的连接入口恢复。R01/R02/R04/I01 仍缺浏览器和真实 Token 到 Agent 的端到端验收。
2. 运维基础闭环：A01 单资产生命周期；A02 rollout 暂停/恢复及批量逐项进度；A03 VM 诊断；O01 差异预览与显式对账；O02 任务取消、重试资格与阻塞原因；O03 全局 runtime/资产组合检索。
3. 文件与访问：A04 容器文件管理/VM SFTP；A05 VNC；A07 端口映射的分配、冲突、实际应用、撤销与恢复；R05 真实审计文件的授权下载、配额和保留清理。
4. 状态保存与虚实结合：A06 快照/回滚；H01 Connector provider 真实生命周期；H02 设备包受信制品执行及监督。不能把元数据模型当作执行能力。
5. 验收：本地主站已恢复并运行本轮制品，内部生成客户端已更新；仍需登录后的浏览器四档宽度、日夜主题与键盘，以及完整操作链路验收；随后在具备条件的授权环境验收多 Worker、KVM/OVN、物理端点的互通、隔离、重启和销毁。不包含 PVE 管理面。

- 按垂直功能执行定向测试，并完成受影响前后端全量门禁。
- 前端检查 390、1366、1920、2560 宽度、日夜主题、键盘、减少动效。
- 数据迁移使用 PostgreSQL 隔离副本验证前向升级与恢复。
- 当前远端不可达；本地模拟不能等价验证 KVM、OVN/OVS 多 Worker 和物理端点。
- 保留用户已有工作区改动；指定完整交接文档包含凭据，禁止提交。
- 此清单随实现和测试逐项更新，未通过验证不得标记完成。

## 2026-09-07 本地验证记录

- 分支未提交、未推送、未生产部署。CodeGraph 已按用户授权初始化。
- `dotnet build src/GZCTF.slnx -c Release --no-restore` 通过。首次因 AppHost 尚未 restore 失败，restore 后通过。
- 后端全量单元测试最新：945/945；后续跨批次对账、过期清理与终端 registry 专项：11/11。
- 前端全量：281/281，locales、lint、TypeScript、architecture、Vite build 和 bundle budget 通过，包含后续终端主题/积压控制变更。
- 集成测试：261/272，11 项失败，禁止作为发布通过：OpenAPI 文档/规范快照 2 项、写操作幂等要求 1 项、开放 TeamLab DTO 暴露 `lastError` 1 项、镜像凭据认证能力旧入口返回 405 共 7 项。尚未在干净基线隔离复跑，不能直接归为历史问题。
- 新增 `scripts/testing/test-teamlab-terminal-local.ps1`，只访问本机模拟 Agent，使用独立无网络临时容器；凭据仅内存读取，不输出、不落盘。已通过真实终端输入、UTF-8、resize、Ctrl-C、inventory、取消及 shell 进程消失检查。测试容器及其匿名卷在 finally 中删除，不影响已有数据库容器。
- Docker.DotNet 3.125.15 的 HTTP 200 回退不能正确完成本地 stdin 转发，而其错误处理又将 101 升级响应视为异常。终端 attach 改用 .NET `SocketsHttpHandler` 的 101 双向流，exec 创建和 resize 仍复用 Docker SDK。结束时按随机 exec 环境标识清理本容器终端进程，等待退出；失败可重试，不记录终端输入内容。
- 后续终端协议/会话 registry/Guacamole 专项测试：21/21。
- 终端 registry 保留租约到期后的待清理记录，过期取消仍等待执行退出；并发重试串行化，成功清理不重复执行。校验或 WebSocket 升级阶段失败且尚未注册执行资源时，不再错误滞留清理状态。镜像缺少 shell、tr、grep、kill 或可读 proc 时拒绝启动终端。模拟 Agent 已更新并再次通过真实 Docker 终端脚本；缺工具拒绝路径尚未单独做真实容器验收。
- 对账与过期清理改为固定 ID 上界的 keyset 分批遍历，不再由前 500 条存活会话或前 100 条清理失败记录阻塞后续记录。501 条对账及 101 条清理回归已通过；单个清理异常不再中断整轮对账。
- 模拟 Agent 基于 Alpine，需发布 `linux-musl-x64` 自包含制品；已保留更新前二进制于仓库外临时目录。主站仍是此前运行制品，未热切换后端。

## 未完成的关键工作

### O03 全局实例检索增量

- 保留原试运行列表语义，新增 `/api/admin/teamlab/runtimes/search`。管理员可检索全部业务来源，非管理员限定 CreatedById；不把其他用户的实例因搜索参数暴露出来。支持引用/资产名与 key、精确 runtime UUID、状态、节点名称、代次、release、创建人和异常筛选。
- 节点、资产和资产错误仅使用当前代次；实例 Failed/CleanupPending 即使缺少 LastError 也归入异常结果。创建时间 + UUID 游标分页，不新增索引表或搜索事实源。
- 场景库提供场景/实例/会话视图切换；筛选区响应式布局，结果可进入现有资产运维并返回实例检索。rollout 托管身份加入 runtime 投影，详情禁用直接重置/暂停/恢复/销毁/清理，避免呈现后端必然拒绝的操作；后端原有权限与归属保护不变。
- 后端全量单元测试 1047/1047，新增搜索专项 5/5；真实 PostgreSQL 专项 1/1，覆盖 SQL 翻译、组合筛选、同时间戳分页、当前代次与所有者过滤。首次 PostgreSQL fixture 的 CanonicalJson 为空导致初始化失败，补为合法 JSON 后重跑通过。
- 前端新增筛选提交、运维往返和托管实例按钮禁用回归，301/301；当前主站已更新，路由可见、匿名访问 401，内部客户端重新生成。生成后的 locales、lint、TypeScript、architecture、全量测试、Vite 构建和 bundle budget 全部通过；全量集成 278/278（4 分 42 秒），退出码 0。登录后多尺寸浏览器和大规模性能验收未完成。
- 非管理员对“本人创建之外、通过其他模块授权管理”的实例尚不纳入此搜索范围，仍使用各业务入口访问；未扩大其查询权限。未提交、推送或生产部署。

### A03 VM 电源诊断增量

- 新增 VM 专用诊断 DTO/API/面板，不复用容器退出码、日志等不适用字段。显示 libvirt 电源状态、域 UUID 和采集时间，不把电源状态视作来宾健康。
- 主站从绑定资产读取域名称、代次、UUID，要求运维权限；采集后复查 UUID、节点、资源名称及代次，并再次授权。成功读取记入既有事件审计。没有真实 UUID 的旧资产返回身份不完整，不按域名称猜测归属。
- Agent 固定使用 `virsh dumpxml/domstate` 的参数列表，查询前后均核对 XML 中名称、UUID、代次；禁止 DTD。整个请求 10 秒限时，stdout/stderr 各限制 512 Ki 字符，异常/取消时回收子进程，不输出原始 XML 或 stderr。无 libvirt 工具返回 `diagnostics.kvm_unavailable`。
- 修复 V2 inventory 回写缺口：Agent inventory 增加可选 NativeIdentity 并返回实际域 UUID；主站不再把 VM 域名称当作 NativeIdentity。旧 Agent 缺少该字段时保持未知身份，而不是伪造 UUID。已有旧记录尚未执行身份恢复。
- 后端全量单元测试 1042/1042（含身份、XML/DTD、输出边界和主站改绑拒绝）。本地 Agent 已更新，真实调用验证缺少 libvirt 的明确失败；Docker 诊断再次通过。当前本地不具备真实 KVM，不能签收 VM 查询、进程超时回收或 V2 UUID 原生调用的实机链路。
- 本地主站已切到新的独立临时制品目录，内部 TypeScript 客户端已重新生成；本轮未重跑全量集成或登录后浏览器验收。未提交、推送或生产部署。
- 生成后前端完整门禁通过，298/298 测试。Docker 终端回归曾因脚本在 sleep 启动前发送 Ctrl-C、未等提示符恢复便发送下一命令而失败；测试改为观察前台进程启动及提示符后再继续，重跑通过，未放宽生产行为断言。测试临时容器已清理。

### O02 历史任务增量

- 新增按 runtime 隔离的任务历史查询，复用 DeploymentQueueTicket；支持所有代次/指定代次，按 CreatedAt + Id 倒序游标分页。同时间戳分页、不重复/漏项、跨 runtime 隔离、代次筛选和非法分页参数专项 4/4 通过。
- Controller 使用既有 MetadataRead 授权；只选择任务标识、代次、操作、状态、阶段、时间、错误码/阻塞码和重试资格，不读取返回 ProtectedPayload、原始错误文本、ClaimOwner 等内部字段。
- 运行详情“事件与日志”增加任务历史，支持当前代次筛选、上一页/下一页、刷新、错误态和空态；runtime 或代次变化重建分页状态，读取失败不展示旧页数据。前端两项交互回归通过。
- 后端全量单元测试 1032/1032；前端 296/296 与完整构建通过。主站使用仓库外独立运行制品目录，保持源码目录作为 content root 与原数据库/存储配置；OpenAPI 含任务历史路由，匿名访问真实返回 401。内部客户端已从当前主站重新生成。
- 本轮没有执行全量集成测试、PostgreSQL 并发分页和登录后浏览器多尺寸验收；任务历史不能视为运行中协作取消已实现。未提交、推送或生产部署。

### O02 任务诊断与取消结果增量

- 管理端取消接口在调用统一队列服务后重新读取票据；只有实际 Cancelled 才返回 204 并记录取消成功。运行中返回 409 `ticket_running`，其他非取消终态返回 `ticket_not_cancellable`，不再把忽略取消报告为成功。本轮没有实现运行中协作取消。
- TeamLab feature adapter 不再丢弃已有的 currentOperationId、subStages 和 failure；新增纯展示运行任务区，显示真实票据状态、阶段消息、错误分类和重试资格，并跳转当前代次的事件查询。不会伪造重试成功或提供无执行底座的重试按钮。
- 关闭开发环境默认 EF 敏感值日志；保留详细错误结构，不将敏感参数值主动写入日志。
- 后端全量单元测试 1028/1028；前端 locales、lint、TypeScript、architecture、294/294 测试、Vite 构建和体积门禁通过。未重跑本轮集成测试，此前 277/277 不作为新增取消响应的真实 HTTP/竞态验收；登录后浏览器、多尺寸验收仍待补。
- 本地构建曾因正在运行的 apphost 文件锁失败，停止本地主站后重跑通过；改用 dotnet DLL 启动本地服务，不删除任何数据卷。未提交、推送或生产部署。
- 取消专项随后加强为实际调用 Controller + DeploymentQueueService（InMemory DB），运行中/成功/失败票据均返回 409 且保留原状态，3/3 通过；不等同于真实 HTTP 授权或 PostgreSQL 并发验收。主站已重启，OpenAPI 冒烟返回 200。

### 重启后恢复与 A02 增量

- 2026-09-07 电脑重启后确认数据库、Redis、guacd、模拟 Agent 均已重新运行；未删除数据卷。恢复 Vite `127.0.0.1:63000` 和本地主站 `localhost:8080`；实时 OpenAPI 返回 200，包含资产诊断与暂停接口。首页赛事/通知可读取，课程目录仍显示加载失败，运维页面等待本地测试管理员登录后验收。
- Open/rollout 的暂停恢复不再由 operation worker 直接执行，改为关联现有 DeploymentQueueTicket 并等待真实结果；恢复执行时查回原 operation 票据。rollout 身份使用既有受保护 payload，入队和执行都核对成员关系；代次拒绝继续复用原执行检查，没有新表或新队列。
- 新增 rollout 正常入队/执行、拒绝越界、执行时重查归属及暂停恢复阶段展示测试。后续连同启动日志修复，后端全量单元测试 1025/1025。
- 删除数据库启动失败时完整连接串日志，失败提示仅保留异常类型，不记录异常正文或 inner exception；新增敏感信息不进入失败分类的回归测试。
- `pnpm genapi` 从实际主站重生成内部客户端；历史客户端存在过时队列枚举和测试专用路由，生成差异较大。未手改生成文件；前端 locales、lint、TypeScript、architecture、290/290 测试、Vite 构建和 bundle budget 全部通过。
- 此轮集成测试重新执行完成，277/277，耗时 4 分 22 秒，退出码 0；重启前中断的那次没有最终结果，不作验收依据。重启后的真实 Docker 终端与诊断脚本均再次通过，测试临时容器已清理，四个持久模拟服务仍运行。`git diff --check` 通过。仍未提交、推送或生产部署，A02 不等同于批量 UI 或真实 KVM/OVN 验收完成。

### A03 Docker 诊断增量

- 管理 API 经 application/gateway 到 Agent，仅允许授权 runtime 的绑定资产；校验容器归属与当前代次，采集后再检查绑定，拒绝重建期间的旧结果。只返回白名单运行状态，不返回 inspect 环境变量、挂载或配置。
- 日志最多 1000 行、64 KiB，10 秒采集期限；中文截断不制造替换字符。日志驱动不可读或日志超时不丢弃已经采集的实际状态，以独立错误码提示，取消仍向上传播。
- 资产运维页包含容器选择、行数、刷新、状态、退出码、重启次数、启动/退出/采集时间、有界纯文本输出、空态与错误态。请求按 runtime/代次/资产隔离，不保留上一资产输出。
- 最新后端全量单元测试 1012/1012；本地真实 Docker 脚本验证状态、中文、输出上限、跨 runtime 与旧代次拒绝，以及 `none` 日志驱动的状态保留。两只隔离临时容器及其匿名卷已清理，不影响数据库。Agent 本地模拟制品已更新。
- 前端完整 `pnpm build` 通过：locales、lint、TypeScript、architecture、290/290 测试、Vite 构建与 bundle budget；修正新增测试样本的必填字段及全局 DOM 查询后重跑通过。`git diff --check` 通过。
- 后续修复：采集后核对绑定节点，并重新检查读取权限；成功返回前复用 TeamLabEventRecorder 持久化访问审计，只保存资产/操作者/代次/行数/截断/日志可用性，不复制正文。节点改绑、权限撤销、审计元数据回归已通过，诊断专项 6/6。该成功读取审计不等同于 R05 远程审计文件能力。
- 此次后续修复的后端全量单元测试 1015/1015，`git diff --check` 通过；未修改前端或 Agent，未重跑前端、集成和真实容器验收。主站未更新，未提交、推送或部署。
- 本轮未重新执行集成测试；上次 277/277 不含此次诊断增量。主站仍为旧运行制品，内部生成客户端、浏览器四档宽度/主题/键盘及主站到 Agent 全链路未验收，不得据此标记 A03 完成。

本次后续验证：快进同步 main `51c2884` 后，后端全量单元测试 1006/1006，远程创建恢复/代次及 operation submission 专项 13/13；外部契约生成测试通过，向后兼容检查通过。全量集成测试首次长时间未返回而取消，随后增加详细进度和 90 秒无进展诊断重新执行，277/277 通过，用时约 4.8 分钟，退出码 0。先前 11 项失败记录属于旧基线，最新运行不再失败；其中镜像认证入口及通用契约测试修订来自同步的 main，不能归为本轮功能修复。本次未重新运行前端全量门禁，主站运行制品仍未更新，不能将旧服务当作新代码验收。

测试环境残余：集成测试的容器销毁发生在测试宿主停止之前，后台采样服务会出现数据库连接拒绝日志；本轮未修改该通用测试设施。真实 Token 创建、轮询、连接与关闭到 Agent 的全链路仍需单独验收，现有门禁不替代该验收。

- A01、A03-A07、R05、O01-O03、H01-H03 未完整实施，不能宣称产品地图已闭环。
- Open 远程创建/关闭已复用现有 operation submission store、worker 和受保护 payload，增加独立外部 DTO，创建身份复用 operation ID，清理待完成通过现有 defer 机制重试，没有新增幂等表或独立队列。生成 OpenAPI 已通过向后兼容检查，内部客户端已重生成并通过前端门禁；真实 Token 到 Agent 的完整验收仍待补。
- 远程 inventory 固定批次饥饿已修复；节点失联状态、清理退避、主站重启后一次性连接 URL 恢复仍待补。Agent 重启后的终端进程清理身份尚未持久恢复；失败清理记录保留在内存，不等价于重启恢复闭环。
- 尚未完成浏览器多分辨率/主题/键盘检查，以及 Linux 多 Worker、KVM/OVN 和物理设备真实验收。
