# 联赛 T0、T1、T4 实施记录

起点：`origin/main 9999e6b1`。任务分支：`codex/league-phase1-lxy-20260921`。

已阅读 2026-09-21 分工手册。当前主线没有 League 模块；现有 Team/User、TeamLab release、统一部署队列和 PostgreSQL 事务可以复用。用户已确认独立保存联赛报名、队长报名、准备时冻结成员，以及由 lxy 先提供共享 C# 契约。

## 工作安排

1. T0：已定义场次、报名、冻结配置、运行/Flag/初始金币端口和同库终局契约，完成模块注册及迁移。
2. T1：已实现管理员创建和配置、列表详情、队长报名、审核及两队选择。
3. T4：已实现持久化准备/开赛/清理意图、后台恢复和重试；终局使用数据库行锁，支持 lmr 的外层事务。
4. 已执行单元测试、PostgreSQL 迁移及事务/重启/并发集成测试、Release 构建和现有回归；具体结果以任务交接为准。
5. 接口样例和待联调项已写入第一阶段契约。代码只提交到任务分支，不合并、不部署。

## 验证边界

测试替身仅存在于测试项目。T2/T3/T6/T7 未接入时返回明确的依赖错误；配置默认关闭联赛入口。真实场景、Flag 注入、访问隔离和回收由后续联调确认。Docker 启动后已完成独立 Testcontainers 验证。
