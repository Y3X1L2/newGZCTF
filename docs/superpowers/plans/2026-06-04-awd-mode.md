# AWD 攻防赛模式 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `subagent-driven-development` (recommended) or `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 NEWGZCTF 平台实现 AWD（Attack With Defense）攻防赛模式，作为独立于原生 Jeopardy CTF 的第三种比赛模式，支持 `Mixed` 模式（一场比赛同时包含 CTF + AWD）。

**Architecture:** AWD 核心模型独立（`AwdService`, `AwdRound`, `AwdFlag` 等），但最大化复用原生周边基础设施（`Game`, `Team`, `Container`, `Submission`, `FirstSolve`, `GameEvent`, SignalR）。前端复用 Mantine 主题和原生布局组件。

**Tech Stack:** ASP.NET Core 10 + EF Core + Docker.DotNet + SignalR + React 19 + Mantine v9 + TypeScript

---

## 文件结构映射

### 后端新增文件

| 文件 | 职责 |
|------|------|
| `src/GZCTF/Models/Data/AwdService.cs` | AWD 服务定义实体 |
| `src/GZCTF/Models/Data/AwdServiceInstance.cs` | 队伍靶机实例实体 |
| `src/GZCTF/Models/Data/AwdRound.cs` | 轮次管理实体 |
| `src/GZCTF/Models/Data/AwdFlag.cs` | 每轮每队 Flag 实体 |
| `src/GZCTF/Models/Data/AwdCheckerTask.cs` | Checker 执行结果实体 |
| `src/GZCTF/Utils/Enums.cs` | 扩展 `GameType`, `EventType`, `ChallengeType`, `AwdRoundStatus`, `CheckerStatus` |
| `src/GZCTF/Services/AwdInstanceService.cs` | 靶机实例生命周期管理（创建/销毁/重置） |
| `src/GZCTF/Services/AwdRoundService.cs` | 轮次驱动 HostedService |
| `src/GZCTF/Services/AwdCheckerService.cs` | Checker 执行与 SLA 计算 |
| `src/GZCTF/Services/AwdScoreService.cs` | AWD 得分计算与写入原生排行榜 |
| `src/GZCTF/Controllers/AwdAdminController.cs` | 管理后台 API |
| `src/GZCTF/Controllers/AwdPlayerController.cs` | 参赛者 API |
| `src/GZCTF/Repositories/AwdRepository.cs` | AWD 数据访问层 |
| `src/GZCTF/Models/Request/Game/AwdServiceModels.cs` | AWD 请求/响应 DTO |

### 后端修改文件

| 文件 | 修改内容 |
|------|----------|
| `src/GZCTF/Models/Data/Game.cs` | 增加 `GameType` 属性 |
| `src/GZCTF/Models/Data/Challenge.cs` | 增加 `ChallengeType.AWDService` |
| `src/GZCTF/Models/Data/GameEvent.cs` | 扩展 `EventType` 枚举 |
| `src/GZCTF/Models/AppDbContext.cs` | 注册 AWD 实体到 DbContext |
| `src/GZCTF/Hubs/Clients/IMonitorClient.cs` | 增加 AWD SignalR 事件方法 |
| `src/GZCTF/ClientApp/src/components/WithGameTab.tsx` | 条件渲染 AWD Tab |
| `src/GZCTF/ClientApp/src/components/admin/WithGameEditTab.tsx` | 条件渲染 AWD 管理 Tab |

### 前端新增文件

| 文件 | 职责 |
|------|------|
| `src/GZCTF/ClientApp/src/pages/games/[id]/Awd.tsx` | 参赛者 AWD 主页面 |
| `src/GZCTF/ClientApp/src/pages/admin/games/[id]/Awd.tsx` | 管理员 AWD 控制面板 |
| `src/GZCTF/ClientApp/src/components/awd/AwdStatusMatrix.tsx` | 服务状态矩阵 |
| `src/GZCTF/ClientApp/src/components/awd/AwdFlagSubmit.tsx` | Flag 提交组件 |
| `src/GZCTF/ClientApp/src/components/awd/AwdAttackLog.tsx` | 攻击记录流 |
| `src/GZCTF/ClientApp/src/components/awd/AwdScoreboard.tsx` | AWD 排行榜（CTF+AWD 双列） |
| `src/GZCTF/ClientApp/src/components/awd/AwdRoundTimer.tsx` | 轮次倒计时 |
| `src/GZCTF/ClientApp/src/components/awd/admin/AwdServiceManager.tsx` | 服务管理 |
| `src/GZCTF/ClientApp/src/components/awd/admin/AwdInstanceGrid.tsx` | 靶机监控网格 |
| `src/GZCTF/ClientApp/src/components/awd/admin/AwdGameControl.tsx` | 比赛控制面板 |

---

## Task 1: 扩展原生枚举与 Game 模型

**目标：** 为 AWD 模式扩展必要的枚举值和原生模型字段。

**Files:**
- Modify: `src/GZCTF/Utils/Enums.cs`
- Modify: `src/GZCTF/Models/Data/Game.cs`
- Modify: `src/GZCTF/Models/Data/Challenge.cs`
- Modify: `src/GZCTF/Models/Data/GameEvent.cs`

- [ ] **Step 1: 扩展 `GameType` 枚举**

在 `src/GZCTF/Utils/Enums.cs` 中找到 `GameType` 枚举定义（如果不存在则新建）：

```csharp
public enum GameType
{
    [Description("Jeopardy")]
    Jeopardy = 0,

    [Description("AWD")]
    AWD = 1,

    [Description("Theory")]
    Theory = 2,

    [Description("Mixed")]
    Mixed = 3
}
```

- [ ] **Step 2: 扩展 `ChallengeType` 枚举**

在同一文件中，扩展 `ChallengeType`：

```csharp
public enum ChallengeType
{
    // ... 现有值 ...
    AWDService = 0b10000  // 16
}
```

并更新 `ChallengeTypeExtensions`：

```csharp
public static class ChallengeTypeExtensions
{
    // ... 现有方法 ...

    public static bool IsAwdService(this ChallengeType type) => type == ChallengeType.AWDService;
}
```

- [ ] **Step 3: 扩展 `EventType` 枚举**

```csharp
public enum EventType
{
    // ... 现有值（最大到 9 左右）...
    AwdFlagSubmit = 20,
    AwdServiceUp = 21,
    AwdServiceDown = 22,
    AwdServiceMumble = 23,
    AwdRoundStart = 24,
    AwdAttackSuccess = 25
}
```

- [ ] **Step 4: 为 `Game` 模型增加 `GameType` 属性**

修改 `src/GZCTF/Models/Data/Game.cs`：

```csharp
public class Game
{
    // ... 现有字段 ...

    public GameType GameType { get; set; } = GameType.Jeopardy;
}
```

- [ ] **Step 5: 编译验证**

Run: `dotnet build src/GZCTF/GZCTF.csproj`
Expected: 成功，无错误

- [ ] **Step 6: Commit**

```bash
git add src/GZCTF/Utils/Enums.cs src/GZCTF/Models/Data/Game.cs src/GZCTF/Models/Data/Challenge.cs src/GZCTF/Models/Data/GameEvent.cs
git commit -m "feat(awd): add GameType, ChallengeType.AWDService, and AWD EventType enums"
```

---

## Task 2: 创建 AWD 核心实体

**目标：** 创建 AWD 所需的 5 个核心实体类。

**Files:**
- Create: `src/GZCTF/Models/Data/AwdService.cs`
- Create: `src/GZCTF/Models/Data/AwdServiceInstance.cs`
- Create: `src/GZCTF/Models/Data/AwdRound.cs`
- Create: `src/GZCTF/Models/Data/AwdFlag.cs`
- Create: `src/GZCTF/Models/Data/AwdCheckerTask.cs`

- [ ] **Step 1: 创建 `AwdService` 实体**

`src/GZCTF/Models/Data/AwdService.cs`：

```csharp
using System.ComponentModel.DataAnnotations;

namespace GZCTF.Models.Data;

public class AwdService
{
    [Key]
    public int Id { get; set; }

    public int GameId { get; set; }
    public Game Game { get; set; } = null!;

    [Required]
    public string Name { get; set; } = string.Empty;

    [Required]
    public string ImageName { get; set; } = string.Empty;

    public int ExposePort { get; set; }

    public string? CheckerScript { get; set; }
    public string? CheckerEntrypoint { get; set; } = "python checker.py";

    public int OriginalScore { get; set; } = 1000;
    public int AttackPoints { get; set; } = 50;
    public int SlaPoints { get; set; } = 20;
    public int MaxAttackPerRound { get; set; } = 3;

    public int RoundDurationMinutes { get; set; } = 5;
    public int TotalRounds { get; set; } = 20;

    public List<AwdServiceInstance> Instances { get; set; } = [];
    public List<AwdRound> Rounds { get; set; } = [];
}
```

- [ ] **Step 2: 创建 `AwdServiceInstance` 实体**

`src/GZCTF/Models/Data/AwdServiceInstance.cs`：

```csharp
using System.ComponentModel.DataAnnotations;

namespace GZCTF.Models.Data;

public class AwdServiceInstance
{
    [Key]
    public int Id { get; set; }

    public int ServiceId { get; set; }
    public AwdService Service { get; set; } = null!;

    public int TeamId { get; set; }
    public Team Team { get; set; } = null!;

    public int? ContainerId { get; set; }
    public Container? Container { get; set; }

    public string NetworkName { get; set; } = string.Empty;
    public bool IsRunning { get; set; }
    public DateTimeOffset CreatedAt { get; set; } = DateTimeOffset.UtcNow;
}
```

- [ ] **Step 3: 创建 `AwdRound` 实体**

`src/GZCTF/Models/Data/AwdRound.cs`：

```csharp
using System.ComponentModel.DataAnnotations;

namespace GZCTF.Models.Data;

public enum AwdRoundStatus
{
    Preparing,
    Running,
    Finished
}

public class AwdRound
{
    [Key]
    public int Id { get; set; }

    public int GameId { get; set; }
    public Game Game { get; set; } = null!;

    public int RoundNumber { get; set; }
    public DateTimeOffset StartTime { get; set; }
    public DateTimeOffset? EndTime { get; set; }
    public AwdRoundStatus Status { get; set; } = AwdRoundStatus.Preparing;

    public List<AwdFlag> Flags { get; set; } = [];
    public List<AwdCheckerTask> CheckerTasks { get; set; } = [];
}
```

- [ ] **Step 4: 创建 `AwdFlag` 实体**

`src/GZCTF/Models/Data/AwdFlag.cs`：

```csharp
using System.ComponentModel.DataAnnotations;

namespace GZCTF.Models.Data;

public class AwdFlag
{
    [Key]
    public int Id { get; set; }

    public int RoundId { get; set; }
    public AwdRound Round { get; set; } = null!;

    public int ServiceId { get; set; }
    public AwdService Service { get; set; } = null!;

    public int TeamId { get; set; }
    public Team Team { get; set; } = null!;

    [Required]
    public string FlagValue { get; set; } = string.Empty;

    public bool IsSubmitted { get; set; }
    public DateTimeOffset? FirstSubmittedAt { get; set; }
}
```

- [ ] **Step 5: 创建 `AwdCheckerTask` 实体**

`src/GZCTF/Models/Data/AwdCheckerTask.cs`：

```csharp
using System.ComponentModel.DataAnnotations;

namespace GZCTF.Models.Data;

public enum CheckerStatus
{
    OK,
    Mumble,
    Down,
    Corrupt
}

public class AwdCheckerTask
{
    [Key]
    public int Id { get; set; }

    public int RoundId { get; set; }
    public AwdRound Round { get; set; } = null!;

    public int ServiceId { get; set; }
    public AwdService Service { get; set; } = null!;

    public int TeamId { get; set; }
    public Team Team { get; set; } = null!;

    public CheckerStatus Status { get; set; }
    public string? Message { get; set; }
    public DateTimeOffset ExecutedAt { get; set; } = DateTimeOffset.UtcNow;
}
```

- [ ] **Step 6: 注册实体到 `AppDbContext`**

修改 `src/GZCTF/Models/AppDbContext.cs`，在 `OnModelCreating` 或 DbSet 属性区域添加：

```csharp
public DbSet<AwdService> AwdServices { get; set; } = null!;
public DbSet<AwdServiceInstance> AwdServiceInstances { get; set; } = null!;
public DbSet<AwdRound> AwdRounds { get; set; } = null!;
public DbSet<AwdFlag> AwdFlags { get; set; } = null!;
public DbSet<AwdCheckerTask> AwdCheckerTasks { get; set; } = null!;
```

- [ ] **Step 7: 编译验证**

Run: `dotnet build src/GZCTF/GZCTF.csproj`
Expected: 成功，无错误

- [ ] **Step 8: Commit**

```bash
git add src/GZCTF/Models/Data/Awd*.cs src/GZCTF/Models/AppDbContext.cs
git commit -m "feat(awd): add AWD core entities (Service, Instance, Round, Flag, CheckerTask)"
```

---

## Task 3: 生成 EF Core Migration

**目标：** 为 AWD 实体和 GameType 字段创建数据库迁移。

**Files:**
- Create: `src/GZCTF/Migrations/xxxx_AddAwdModeSupport.cs`（自动生成）

- [ ] **Step 1: 生成 Migration**

Run:
```bash
cd src/GZCTF
dotnet ef migrations add AddAwdModeSupport --context AppDbContext
```
Expected: 成功生成迁移文件

- [ ] **Step 2: 检查生成的迁移文件**

确认迁移文件包含：
- `Games` 表新增 `GameType` 列（int, default 0）
- 新建 `AwdServices`, `AwdServiceInstances`, `AwdRounds`, `AwdFlags`, `AwdCheckerTasks` 表
- 外键关系正确

- [ ] **Step 3: Commit**

```bash
git add src/GZCTF/Migrations/
git commit -m "feat(awd): add EF Core migration for AWD mode support"
```

---

## Task 4: AWD 请求/响应 DTO

**目标：** 定义前后端交互的数据传输对象。

**Files:**
- Create: `src/GZCTF/Models/Request/Game/AwdServiceModels.cs`

- [ ] **Step 1: 创建 DTO 文件**

`src/GZCTF/Models/Request/Game/AwdServiceModels.cs`：

```csharp
using System.ComponentModel.DataAnnotations;

namespace GZCTF.Models.Request.Game;

public class AwdServiceCreateModel
{
    [Required]
    public string Name { get; set; } = string.Empty;

    [Required]
    public string ImageName { get; set; } = string.Empty;

    public int ExposePort { get; set; } = 80;
    public string? CheckerScript { get; set; }
    public string? CheckerEntrypoint { get; set; } = "python checker.py";
    public int AttackPoints { get; set; } = 50;
    public int SlaPoints { get; set; } = 20;
    public int MaxAttackPerRound { get; set; } = 3;
    public int RoundDurationMinutes { get; set; } = 5;
    public int TotalRounds { get; set; } = 20;
}

public class AwdServiceUpdateModel : AwdServiceCreateModel
{
}

public class AwdServiceViewModel
{
    public int Id { get; set; }
    public string Name { get; set; } = string.Empty;
    public string ImageName { get; set; } = string.Empty;
    public int ExposePort { get; set; }
    public int AttackPoints { get; set; }
    public int SlaPoints { get; set; }
    public int RoundDurationMinutes { get; set; }
    public int TotalRounds { get; set; }
}

public class AwdSubmitModel
{
    [Required]
    public string Flag { get; set; } = string.Empty;

    [Required]
    public int TargetTeamId { get; set; }

    [Required]
    public int ServiceId { get; set; }
}

public class AwdGameStatusModel
{
    public int GameId { get; set; }
    public int CurrentRound { get; set; }
    public DateTimeOffset RoundStartTime { get; set; }
    public int RoundDurationMinutes { get; set; }
    public AwdRoundStatus Status { get; set; }
}

public class AwdServiceStatusModel
{
    public int ServiceId { get; set; }
    public string ServiceName { get; set; } = string.Empty;
    public List<TeamServiceStatus> TeamStatuses { get; set; } = [];
}

public class TeamServiceStatus
{
    public int TeamId { get; set; }
    public string TeamName { get; set; } = string.Empty;
    public string? IpAddress { get; set; }
    public int? Port { get; set; }
    public CheckerStatus? LastCheckerStatus { get; set; }
    public bool IsRunning { get; set; }
}

public class AwdScoreboardItem
{
    public int Rank { get; set; }
    public int TeamId { get; set; }
    public string TeamName { get; set; } = string.Empty;
    public int CtfScore { get; set; }
    public int AwdScore { get; set; }
    public int TotalScore => CtfScore + AwdScore;
    public int AttackScore { get; set; }
    public int SlaScore { get; set; }
    public int DefenseLost { get; set; }
}

public class AwdAttackLogItem
{
    public DateTimeOffset Time { get; set; }
    public string AttackerTeam { get; set; } = string.Empty;
    public string VictimTeam { get; set; } = string.Empty;
    public string ServiceName { get; set; } = string.Empty;
    public int Points { get; set; }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/GZCTF/Models/Request/Game/AwdServiceModels.cs
git commit -m "feat(awd): add AWD request/response DTOs"
```

---

## Task 5: AWD 数据访问层（Repository）

**目标：** 创建 AWD 专用的 Repository，封装数据访问逻辑。

**Files:**
- Create: `src/GZCTF/Repositories/AwdRepository.cs`
- Create: `src/GZCTF/Repositories/Interface/IAwdRepository.cs`

- [ ] **Step 1: 创建 `IAwdRepository` 接口**

`src/GZCTF/Repositories/Interface/IAwdRepository.cs`：

```csharp
namespace GZCTF.Repositories.Interface;

public interface IAwdRepository : IRepository
{
    Task<AwdService?> GetService(int serviceId, CancellationToken token = default);
    Task<AwdService[]> GetServicesByGame(int gameId, CancellationToken token = default);
    Task<AwdServiceInstance?> GetInstance(int instanceId, CancellationToken token = default);
    Task<AwdServiceInstance[]> GetInstancesByGame(int gameId, CancellationToken token = default);
    Task<AwdRound?> GetCurrentRound(int gameId, CancellationToken token = default);
    Task<AwdRound[]> GetRoundsByGame(int gameId, CancellationToken token = default);
    Task<AwdFlag?> GetFlag(int roundId, int serviceId, int teamId, CancellationToken token = default);
    Task<AwdFlag?> GetFlagByValue(string flagValue, CancellationToken token = default);
    Task<AwdCheckerTask[]> GetCheckerTasksByRound(int roundId, CancellationToken token = default);
    Task UpdateFlagSubmitted(int flagId, CancellationToken token = default);
    Task CreateRound(AwdRound round, CancellationToken token = default);
    Task CreateFlags(IEnumerable<AwdFlag> flags, CancellationToken token = default);
    Task CreateCheckerTasks(IEnumerable<AwdCheckerTask> tasks, CancellationToken token = default);
}
```

- [ ] **Step 2: 创建 `AwdRepository` 实现**

`src/GZCTF/Repositories/AwdRepository.cs`：

```csharp
using GZCTF.Repositories.Interface;
using Microsoft.EntityFrameworkCore;

namespace GZCTF.Repositories;

public class AwdRepository(AppDbContext context) : RepositoryBase(context), IAwdRepository
{
    public Task<AwdService?> GetService(int serviceId, CancellationToken token = default)
        => Context.AwdServices.FirstOrDefaultAsync(s => s.Id == serviceId, token);

    public Task<AwdService[]> GetServicesByGame(int gameId, CancellationToken token = default)
        => Context.AwdServices.Where(s => s.GameId == gameId).ToArrayAsync(token);

    public Task<AwdServiceInstance?> GetInstance(int instanceId, CancellationToken token = default)
        => Context.AwdServiceInstances
            .Include(i => i.Container)
            .Include(i => i.Team)
            .FirstOrDefaultAsync(i => i.Id == instanceId, token);

    public Task<AwdServiceInstance[]> GetInstancesByGame(int gameId, CancellationToken token = default)
        => Context.AwdServiceInstances
            .Include(i => i.Service)
            .Include(i => i.Container)
            .Include(i => i.Team)
            .Where(i => i.Service.GameId == gameId)
            .ToArrayAsync(token);

    public Task<AwdRound?> GetCurrentRound(int gameId, CancellationToken token = default)
        => Context.AwdRounds
            .Where(r => r.GameId == gameId && r.Status == AwdRoundStatus.Running)
            .OrderByDescending(r => r.RoundNumber)
            .FirstOrDefaultAsync(token);

    public Task<AwdRound[]> GetRoundsByGame(int gameId, CancellationToken token = default)
        => Context.AwdRounds.Where(r => r.GameId == gameId).OrderBy(r => r.RoundNumber).ToArrayAsync(token);

    public Task<AwdFlag?> GetFlag(int roundId, int serviceId, int teamId, CancellationToken token = default)
        => Context.AwdFlags.FirstOrDefaultAsync(
            f => f.RoundId == roundId && f.ServiceId == serviceId && f.TeamId == teamId, token);

    public Task<AwdFlag?> GetFlagByValue(string flagValue, CancellationToken token = default)
        => Context.AwdFlags
            .Include(f => f.Round)
            .Include(f => f.Service)
            .Include(f => f.Team)
            .FirstOrDefaultAsync(f => f.FlagValue == flagValue, token);

    public Task<AwdCheckerTask[]> GetCheckerTasksByRound(int roundId, CancellationToken token = default)
        => Context.AwdCheckerTasks
            .Where(t => t.RoundId == roundId)
            .ToArrayAsync(token);

    public async Task UpdateFlagSubmitted(int flagId, CancellationToken token = default)
    {
        var flag = await Context.AwdFlags.FindAsync([flagId], token);
        if (flag is not null)
        {
            flag.IsSubmitted = true;
            flag.FirstSubmittedAt = DateTimeOffset.UtcNow;
            await SaveAsync(token);
        }
    }

    public async Task CreateRound(AwdRound round, CancellationToken token = default)
    {
        await Context.AwdRounds.AddAsync(round, token);
        await SaveAsync(token);
    }

    public async Task CreateFlags(IEnumerable<AwdFlag> flags, CancellationToken token = default)
    {
        await Context.AwdFlags.AddRangeAsync(flags, token);
        await SaveAsync(token);
    }

    public async Task CreateCheckerTasks(IEnumerable<AwdCheckerTask> tasks, CancellationToken token = default)
    {
        await Context.AwdCheckerTasks.AddRangeAsync(tasks, token);
        await SaveAsync(token);
    }
}
```

- [ ] **Step 3: 注册 Repository 到 DI**

修改 `src/GZCTF/Program.cs`（或 Service 注册文件），添加：

```csharp
builder.Services.AddScoped<IAwdRepository, AwdRepository>();
```

- [ ] **Step 4: Commit**

```bash
git add src/GZCTF/Repositories/AwdRepository.cs src/GZCTF/Repositories/Interface/IAwdRepository.cs src/GZCTF/Program.cs
git commit -m "feat(awd): add AWD repository and DI registration"
```

---

## Task 6: AwdInstanceService — 靶机实例管理

**目标：** 实现 AWD 靶机实例的创建、销毁、重置，复用原生容器基础设施。

**Files:**
- Create: `src/GZCTF/Services/AwdInstanceService.cs`

- [ ] **Step 1: 创建 `AwdInstanceService`**

`src/GZCTF/Services/AwdInstanceService.cs`：

```csharp
using GZCTF.Models.Data;
using GZCTF.Models.Internal;
using GZCTF.Repositories.Interface;
using GZCTF.Services.Container.Manager;

namespace GZCTF.Services;

public class AwdInstanceService(
    IAwdRepository awdRepository,
    IContainerManager containerManager,
    ContainerOrchestrator containerOrchestrator,
    IContainerRepository containerRepository,
    IGameEventRepository gameEventRepository,
    ILogger<AwdInstanceService> logger)
{
    public async Task CreateInstancesForGame(Game game, CancellationToken token = default)
    {
        var services = await awdRepository.GetServicesByGame(game.Id, token);
        var participations = game.Participations.Where(p => p.Status == ParticipationStatus.Accepted).ToList();

        foreach (var service in services)
        {
            foreach (var part in participations)
            {
                var networkName = $"awd-team-{part.TeamId}";
                await containerOrchestrator.CreateIsolatedNetwork(networkName);

                var container = await containerManager.CreateContainerAsync(new ContainerConfig
                {
                    Image = service.ImageName,
                    ExposePort = service.ExposePort,
                    CpuLimit = 1.0,
                    MemoryLimit = 512,
                    Flag = "PLACEHOLDER", // 将在轮次开始时更新
                    Network = networkName
                }, token);

                if (container is null)
                {
                    logger.LogError("Failed to create container for service {ServiceId}, team {TeamId}",
                        service.Id, part.TeamId);
                    continue;
                }

                await containerRepository.CreateContainer(container);

                var instance = new AwdServiceInstance
                {
                    ServiceId = service.Id,
                    TeamId = part.TeamId,
                    ContainerId = container.Id,
                    NetworkName = networkName,
                    IsRunning = true
                };

                // 使用 DbContext 直接添加（AwdRepository 没有此方法）
                // 实际实现中应通过 AwdRepository 扩展或 DbContext
                // 这里简化处理，后续在 Controller/Service 中通过 DbContext 操作
            }
        }
    }

    public async Task DestroyInstancesForGame(int gameId, CancellationToken token = default)
    {
        var instances = await awdRepository.GetInstancesByGame(gameId, token);
        foreach (var instance in instances)
        {
            if (instance.Container is not null)
            {
                await containerManager.DestroyContainerAsync(instance.Container, token);
            }

            try
            {
                await containerOrchestrator.RemoveNetwork(instance.NetworkName);
            }
            catch (Exception ex)
            {
                logger.LogWarning(ex, "Failed to remove network {Network}", instance.NetworkName);
            }
        }
    }

    public async Task ResetInstance(int instanceId, CancellationToken token = default)
    {
        var instance = await awdRepository.GetInstance(instanceId, token);
        if (instance?.Container is null) return;

        await containerManager.DestroyContainerAsync(instance.Container, token);

        var container = await containerManager.CreateContainerAsync(new ContainerConfig
        {
            Image = instance.Service.ImageName,
            ExposePort = instance.Service.ExposePort,
            Flag = "PLACEHOLDER",
            Network = instance.NetworkName
        }, token);

        if (container is not null)
        {
            await containerRepository.CreateContainer(container);
            instance.ContainerId = container.Id;
            instance.IsRunning = true;
            // Save changes via DbContext
        }
    }
}
```

> **注意**：上面的 `CreateInstancesForGame` 中使用了 `containerRepository.CreateContainer(container)`，但 `container` 是 `IContainerManager.CreateContainerAsync` 返回的 `Models.Data.Container`，需要确认 `IContainerRepository` 是否有对应方法。如果接口不匹配，需要调整为直接使用 `AppDbContext`。

- [ ] **Step 2: 注册到 DI**

在 `Program.cs` 中添加：

```csharp
builder.Services.AddScoped<AwdInstanceService>();
```

- [ ] **Step 3: Commit**

```bash
git add src/GZCTF/Services/AwdInstanceService.cs src/GZCTF/Program.cs
git commit -m "feat(awd): add AwdInstanceService for container lifecycle management"
```

---

## Task 7: AwdRoundService — 轮次驱动 HostedService

**目标：** 实现 AWD 比赛的时间线驱动，每轮生成 Flag、注入容器、触发 Checker、结算得分。

**Files:**
- Create: `src/GZCTF/Services/AwdRoundService.cs`

- [ ] **Step 1: 创建 `AwdRoundService`**

`src/GZCTF/Services/AwdRoundService.cs`：

```csharp
using GZCTF.Models.Data;
using GZCTF.Repositories.Interface;
using GZCTF.Utils;

namespace GZCTF.Services;

public class AwdRoundService : IHostedService, IDisposable
{
    private readonly IServiceProvider _serviceProvider;
    private readonly ILogger<AwdRoundService> _logger;
    private Timer? _timer;
    private readonly Dictionary<int, AwdGameState> _gameStates = new();

    public AwdRoundService(IServiceProvider serviceProvider, ILogger<AwdRoundService> logger)
    {
        _serviceProvider = serviceProvider;
        _logger = logger;
    }

    public Task StartAsync(CancellationToken cancellationToken)
    {
        _logger.LogInformation("AWD Round Service started");
        return Task.CompletedTask;
    }

    public Task StopAsync(CancellationToken cancellationToken)
    {
        _timer?.Change(Timeout.Infinite, 0);
        return Task.CompletedTask;
    }

    public void StartGame(Game game)
    {
        if (_gameStates.ContainsKey(game.Id)) return;

        _gameStates[game.Id] = new AwdGameState
        {
            GameId = game.Id,
            CurrentRound = 0,
            IsRunning = true
        };

        _ = Task.Run(async () => await RunGameLoop(game));
    }

    public void StopGame(int gameId)
    {
        if (_gameStates.TryGetValue(gameId, out var state))
        {
            state.IsRunning = false;
            _gameStates.Remove(gameId);
        }
    }

    private async Task RunGameLoop(Game game)
    {
        using var scope = _serviceProvider.CreateScope();
        var awdRepo = scope.ServiceProvider.GetRequiredService<IAwdRepository>();
        var instanceService = scope.ServiceProvider.GetRequiredService<AwdInstanceService>();
        var checkerService = scope.ServiceProvider.GetRequiredService<AwdCheckerService>();
        var scoreService = scope.ServiceProvider.GetRequiredService<AwdScoreService>();
        var eventRepo = scope.ServiceProvider.GetRequiredService<IGameEventRepository>();

        var services = await awdRepo.GetServicesByGame(game.Id);
        if (services.Length == 0) return;

        var service = services[0]; // 使用第一个服务的配置
        var roundDuration = TimeSpan.FromMinutes(service.RoundDurationMinutes);
        var totalRounds = service.TotalRounds;

        // 预创建实例
        await instanceService.CreateInstancesForGame(game);

        for (int roundNum = 1; roundNum <= totalRounds; roundNum++)
        {
            if (!_gameStates.TryGetValue(game.Id, out var state) || !state.IsRunning)
                break;

            state.CurrentRound = roundNum;

            // 1. 创建轮次记录
            var round = new AwdRound
            {
                GameId = game.Id,
                RoundNumber = roundNum,
                StartTime = DateTimeOffset.UtcNow,
                Status = AwdRoundStatus.Running
            };
            await awdRepo.CreateRound(round);

            // 2. 生成 Flag 并注入
            var participations = game.Participations.Where(p => p.Status == ParticipationStatus.Accepted).ToList();
            var flags = new List<AwdFlag>();

            foreach (var svc in services)
            {
                foreach (var part in participations)
                {
                    var flagValue = FlagHelper.GenerateFlag(32);
                    flags.Add(new AwdFlag
                    {
                        RoundId = round.Id,
                        ServiceId = svc.Id,
                        TeamId = part.TeamId,
                        FlagValue = flagValue
                    });

                    // 注入 Flag 到容器（通过重启容器）
                    var instances = await awdRepo.GetInstancesByGame(game.Id);
                    var instance = instances.FirstOrDefault(i => i.ServiceId == svc.Id && i.TeamId == part.TeamId);
                    if (instance?.Container is not null)
                    {
                        // 更新容器 Flag 并重启
                        await instanceService.ResetInstance(instance.Id);
                        // 注：实际实现中需要在 ResetInstance 中传入新的 Flag 值
                    }
                }
            }

            await awdRepo.CreateFlags(flags);

            // 3. 广播轮次开始事件
            await eventRepo.AddEvent(new GameEvent
            {
                GameId = game.Id,
                Type = EventType.AwdRoundStart,
                Values = [$"Round {roundNum} started"]
            });

            // 4. 执行 Checker
            await checkerService.RunCheckerForRound(round, services, participations);

            // 5. 等待轮次结束
            await Task.Delay(roundDuration);

            // 6. 结算得分
            await scoreService.CalculateRoundScores(round, game);

            round.Status = AwdRoundStatus.Finished;
            round.EndTime = DateTimeOffset.UtcNow;
            // Save round status
        }

        _gameStates.Remove(game.Id);
        _logger.LogInformation("AWD game {GameId} finished", game.Id);
    }

    public void Dispose()
    {
        _timer?.Dispose();
    }
}

public class AwdGameState
{
    public int GameId { get; set; }
    public int CurrentRound { get; set; }
    public bool IsRunning { get; set; }
}
```

- [ ] **Step 2: 注册到 DI**

在 `Program.cs` 中添加：

```csharp
builder.Services.AddHostedService<AwdRoundService>();
```

- [ ] **Step 3: Commit**

```bash
git add src/GZCTF/Services/AwdRoundService.cs src/GZCTF/Program.cs
git commit -m "feat(awd): add AwdRoundService for round-based game loop"
```

---

## Task 8: AwdCheckerService — Checker 执行

**目标：** 实现标准 AWD Checker 协议，执行 Python 脚本检查服务状态。

**Files:**
- Create: `src/GZCTF/Services/AwdCheckerService.cs`

- [ ] **Step 1: 创建 `AwdCheckerService`**

`src/GZCTF/Services/AwdCheckerService.cs`：

```csharp
using System.Diagnostics;
using GZCTF.Models.Data;
using GZCTF.Repositories.Interface;

namespace GZCTF.Services;

public class AwdCheckerService(
    IAwdRepository awdRepository,
    ILogger<AwdCheckerService> logger)
{
    public async Task RunCheckerForRound(AwdRound round, AwdService[] services, List<Participation> participations)
    {
        var tasks = new List<AwdCheckerTask>();

        foreach (var service in services)
        {
            if (string.IsNullOrEmpty(service.CheckerScript))
                continue;

            var instances = await awdRepository.GetInstancesByGame(round.GameId);

            foreach (var part in participations)
            {
                var instance = instances.FirstOrDefault(i => i.ServiceId == service.Id && i.TeamId == part.TeamId);
                if (instance?.Container is null) continue;

                var flag = await awdRepository.GetFlag(round.Id, service.Id, part.TeamId);
                var status = await ExecuteChecker(service, instance, flag?.FlagValue ?? "");

                tasks.Add(new AwdCheckerTask
                {
                    RoundId = round.Id,
                    ServiceId = service.Id,
                    TeamId = part.TeamId,
                    Status = status.Status,
                    Message = status.Message
                });
            }
        }

        await awdRepository.CreateCheckerTasks(tasks);
    }

    private async Task<(CheckerStatus Status, string? Message)> ExecuteChecker(AwdService service, AwdServiceInstance instance, string flagValue)
    {
        try
        {
            var psi = new ProcessStartInfo
            {
                FileName = "python",
                Arguments = $"-c \"{service.CheckerScript}\" {instance.Container.IP} {service.ExposePort} {flagValue}",
                RedirectStandardOutput = true,
                RedirectStandardError = true,
                UseShellExecute = false,
                CreateNoWindow = true
            };

            using var process = Process.Start(psi);
            if (process is null)
                return (CheckerStatus.Down, "Failed to start checker process");

            var timeout = Task.Delay(TimeSpan.FromSeconds(30));
            var completed = await Task.WhenAny(process.WaitForExitAsync(), timeout);

            if (completed == timeout)
            {
                process.Kill();
                return (CheckerStatus.Down, "Checker timeout");
            }

            var output = await process.StandardOutput.ReadToEndAsync();
            var error = await process.StandardError.ReadToEndAsync();
            output = output.Trim().ToUpperInvariant();

            return output switch
            {
                "OK" => (CheckerStatus.OK, null),
                "MUMBLE" => (CheckerStatus.Mumble, error),
                "DOWN" => (CheckerStatus.Down, error),
                "CORRUPT" => (CheckerStatus.Corrupt, error),
                _ => (CheckerStatus.Mumble, $"Unknown output: {output}")
            };
        }
        catch (Exception ex)
        {
            logger.LogError(ex, "Checker execution failed for service {ServiceId}, team {TeamId}",
                service.Id, instance.TeamId);
            return (CheckerStatus.Down, ex.Message);
        }
    }
}
```

- [ ] **Step 2: 注册到 DI**

在 `Program.cs` 中添加：

```csharp
builder.Services.AddScoped<AwdCheckerService>();
```

- [ ] **Step 3: Commit**

```bash
git add src/GZCTF/Services/AwdCheckerService.cs src/GZCTF/Program.cs
git commit -m "feat(awd): add AwdCheckerService with standard checker protocol"
```

---

## Task 9: AwdScoreService — 得分计算与原生排行榜对接

**目标：** 计算每轮 AWD 得分，并写入原生 `Submission` + `FirstSolve`。

**Files:**
- Create: `src/GZCTF/Services/AwdScoreService.cs`

- [ ] **Step 1: 创建 `AwdScoreService`**

`src/GZCTF/Services/AwdScoreService.cs`：

```csharp
using GZCTF.Models.Data;
using GZCTF.Repositories.Interface;

namespace GZCTF.Services;

public class AwdScoreService(
    IAwdRepository awdRepository,
    IGameInstanceRepository gameInstanceRepository,
    IGameEventRepository eventRepository,
    ILogger<AwdScoreService> logger)
{
    public async Task CalculateRoundScores(AwdRound round, Game game)
    {
        var services = await awdRepository.GetServicesByGame(game.Id);
        var participations = game.Participations.Where(p => p.Status == ParticipationStatus.Accepted).ToList();
        var checkerTasks = await awdRepository.GetCheckerTasksByRound(round.Id);
        var flags = await awdRepository.GetFlagsByRound(round.Id); // 需要扩展 Repository 方法

        foreach (var part in participations)
        {
            int attackScore = 0;
            int slaScore = 0;
            int defenseLost = 0;

            foreach (var service in services)
            {
                // 计算 SLA 分
                var task = checkerTasks.FirstOrDefault(t => t.ServiceId == service.Id && t.TeamId == part.TeamId);
                if (task?.Status == CheckerStatus.OK)
                {
                    slaScore += service.SlaPoints;
                }

                // 计算被攻击失分（按攻击次数统计）
                var serviceFlags = flags.Where(f => f.ServiceId == service.Id && f.TeamId == part.TeamId && f.IsSubmitted);
                var attackCount = Math.Min(serviceFlags.Count(), service.MaxAttackPerRound);
                defenseLost += attackCount * service.AttackPoints;
            }

            var totalAwdScore = attackScore + slaScore - defenseLost;

            // 写入原生 Submission + FirstSolve
            // 注意：这里需要为每个 Team 写入一个聚合的 AWD 得分记录
            // 实际实现中可能需要调整数据结构
        }
    }

    public async Task RecordFlagSubmission(int gameId, int attackerTeamId, AwdFlag flag, AwdService service)
    {
        var score = service.AttackPoints;

        // 创建 Submission
        var submission = new Submission
        {
            GameId = gameId,
            TeamId = attackerTeamId,
            UserId = "", // AWD 提交以队伍为单位
            ChallengeId = flag.ServiceId,
            Answer = flag.FlagValue,
            Score = score,
            Result = AnswerResult.Accepted,
            SubmittedAt = DateTimeOffset.UtcNow,
            SubmissionType = ScoringSubmissionType.Flag
        };

        // 写入 FirstSolve（如果不存在）
        // 这里需要复用原生 FirstSolve 逻辑

        await eventRepository.AddEvent(new GameEvent
        {
            GameId = gameId,
            TeamId = attackerTeamId,
            Type = EventType.AwdFlagSubmit,
            Values = [$"+{score} pts", $"Service: {service.Name}"]
        });
    }
}
```

- [ ] **Step 2: 注册到 DI**

在 `Program.cs` 中添加：

```csharp
builder.Services.AddScoped<AwdScoreService>();
```

- [ ] **Step 3: Commit**

```bash
git add src/GZCTF/Services/AwdScoreService.cs src/GZCTF/Program.cs
git commit -m "feat(awd): add AwdScoreService for score calculation and native scoreboard integration"
```

---

## Task 10: AwdAdminController — 管理后台 API

**目标：** 实现管理员对 AWD 比赛的控制 API。

**Files:**
- Create: `src/GZCTF/Controllers/AwdAdminController.cs`

- [ ] **Step 1: 创建 `AwdAdminController`**

`src/GZCTF/Controllers/AwdAdminController.cs`：

```csharp
using GZCTF.Models.Data;
using GZCTF.Models.Request.Game;
using GZCTF.Repositories.Interface;
using GZCTF.Services;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;

namespace GZCTF.Controllers;

[ApiController]
[Route("api/admin/awd")]
[Authorize(Roles = "Admin")]
public class AwdAdminController(
    IAwdRepository awdRepository,
    AwdInstanceService instanceService,
    AwdRoundService roundService,
    IGameRepository gameRepository,
    ILogger<AwdAdminController> logger) : ControllerBase
{
    [HttpGet("games/{gameId}/services")]
    public async Task<IActionResult> GetServices(int gameId)
    {
        var services = await awdRepository.GetServicesByGame(gameId);
        return Ok(services.Select(s => new AwdServiceViewModel
        {
            Id = s.Id,
            Name = s.Name,
            ImageName = s.ImageName,
            ExposePort = s.ExposePort,
            AttackPoints = s.AttackPoints,
            SlaPoints = s.SlaPoints,
            RoundDurationMinutes = s.RoundDurationMinutes,
            TotalRounds = s.TotalRounds
        }));
    }

    [HttpPost("games/{gameId}/services")]
    public async Task<IActionResult> CreateService(int gameId, [FromBody] AwdServiceCreateModel model)
    {
        var game = await gameRepository.GetGameById(gameId);
        if (game is null) return NotFound();

        var service = new AwdService
        {
            GameId = gameId,
            Name = model.Name,
            ImageName = model.ImageName,
            ExposePort = model.ExposePort,
            CheckerScript = model.CheckerScript,
            CheckerEntrypoint = model.CheckerEntrypoint,
            AttackPoints = model.AttackPoints,
            SlaPoints = model.SlaPoints,
            MaxAttackPerRound = model.MaxAttackPerRound,
            RoundDurationMinutes = model.RoundDurationMinutes,
            TotalRounds = model.TotalRounds
        };

        // 通过 DbContext 添加
        // ...

        return Ok(service.Id);
    }

    [HttpPost("games/{gameId}/start")]
    public async Task<IActionResult> StartGame(int gameId)
    {
        var game = await gameRepository.GetGameById(gameId);
        if (game is null) return NotFound();

        roundService.StartGame(game);
        return Ok(new { Message = "AWD game started" });
    }

    [HttpPost("games/{gameId}/stop")]
    public IActionResult StopGame(int gameId)
    {
        roundService.StopGame(gameId);
        return Ok(new { Message = "AWD game stopped" });
    }

    [HttpPost("instances/{instanceId}/reset")]
    public async Task<IActionResult> ResetInstance(int instanceId)
    {
        await instanceService.ResetInstance(instanceId);
        return Ok(new { Message = "Instance reset" });
    }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/GZCTF/Controllers/AwdAdminController.cs
git commit -m "feat(awd): add AwdAdminController for game management APIs"
```

---

## Task 11: AwdPlayerController — 参赛者 API

**目标：** 实现参赛者的 Flag 提交、状态查询、排行榜查看 API。

**Files:**
- Create: `src/GZCTF/Controllers/AwdPlayerController.cs`

- [ ] **Step 1: 创建 `AwdPlayerController`**

`src/GZCTF/Controllers/AwdPlayerController.cs`：

```csharp
using GZCTF.Models.Data;
using GZCTF.Models.Request.Game;
using GZCTF.Repositories.Interface;
using GZCTF.Services;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;

namespace GZCTF.Controllers;

[ApiController]
[Route("api/awd")]
[Authorize]
public class AwdPlayerController(
    IAwdRepository awdRepository,
    AwdScoreService scoreService,
    IGameRepository gameRepository,
    ILogger<AwdPlayerController> logger) : ControllerBase
{
    [HttpGet("games/{gameId}/status")]
    public async Task<IActionResult> GetGameStatus(int gameId)
    {
        var round = await awdRepository.GetCurrentRound(gameId);
        if (round is null)
            return Ok(new { CurrentRound = 0, Status = AwdRoundStatus.Preparing });

        var service = (await awdRepository.GetServicesByGame(gameId)).FirstOrDefault();

        return Ok(new AwdGameStatusModel
        {
            GameId = gameId,
            CurrentRound = round.RoundNumber,
            RoundStartTime = round.StartTime,
            RoundDurationMinutes = service?.RoundDurationMinutes ?? 5,
            Status = round.Status
        });
    }

    [HttpPost("games/{gameId}/submit")]
    public async Task<IActionResult> SubmitFlag(int gameId, [FromBody] AwdSubmitModel model)
    {
        var userId = User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value;
        if (string.IsNullOrEmpty(userId))
            return Unauthorized();

        var flag = await awdRepository.GetFlagByValue(model.Flag);
        if (flag is null || flag.IsSubmitted)
            return BadRequest(new { Message = "Invalid or already submitted flag" });

        if (flag.TeamId == model.TargetTeamId)
            return BadRequest(new { Message = "Cannot submit your own flag" });

        var service = await awdRepository.GetService(flag.ServiceId);
        if (service is null)
            return NotFound();

        await awdRepository.UpdateFlagSubmitted(flag.Id);
        await scoreService.RecordFlagSubmission(gameId, model.TargetTeamId, flag, service);

        return Ok(new { Message = "Flag accepted", Points = service.AttackPoints });
    }

    [HttpGet("games/{gameId}/scoreboard")]
    public async Task<IActionResult> GetScoreboard(int gameId)
    {
        // 调用原生 GenScoreboard 并扩展 AWD 数据
        // 实际实现需要合并 CTF 和 AWD 分数
        return Ok(new List<AwdScoreboardItem>());
    }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/GZCTF/Controllers/AwdPlayerController.cs
git commit -m "feat(awd): add AwdPlayerController for player APIs"
```

---

## Task 12: SignalR 扩展

**目标：** 扩展原生 MonitorHub，增加 AWD 实时事件推送。

**Files:**
- Modify: `src/GZCTF/Hubs/Clients/IMonitorClient.cs`

- [ ] **Step 1: 扩展 `IMonitorClient`**

`src/GZCTF/Hubs/Clients/IMonitorClient.cs`：

```csharp
public interface IMonitorClient
{
    // ... 现有方法 ...

    Task ReceivedAwdRoundChange(AwdGameStatusModel status);
    Task ReceivedAwdFlagSubmit(GameEvent gameEvent);
    Task ReceivedAwdServiceStatusChange(AwdServiceStatusModel status);
}
```

- [ ] **Step 2: Commit**

```bash
git add src/GZCTF/Hubs/Clients/IMonitorClient.cs
git commit -m "feat(awd): extend SignalR MonitorClient with AWD events"
```

---

## Task 13: 前端 WithGameTab 扩展

**目标：** 在原生游戏详情页中条件渲染 AWD Tab。

**Files:**
- Modify: `src/GZCTF/ClientApp/src/components/WithGameTab.tsx`

- [ ] **Step 1: 修改 `WithGameTab`**

在 `pages` 数组中，根据 `game.gameType` 条件添加 AWD Tab：

```tsx
const pages = useMemo(() => {
  const basePages = [
    // ... 现有 Tab ...
  ];

  if (game.gameType === GameType.AWD || game.gameType === GameType.Mixed) {
    basePages.push({
      icon: mdiSwordCross,
      title: t('game.tab.awd'),
      path: 'awd',
      link: 'awd',
      requireJoin: true,
    });
  }

  return basePages;
}, [game.gameType, t]);
```

- [ ] **Step 2: Commit**

```bash
git add src/GZCTF/ClientApp/src/components/WithGameTab.tsx
git commit -m "feat(awd): add AWD tab to WithGameTab for AWD/Mixed games"
```

---

## Task 14: 前端参赛者 AWD 页面

**目标：** 实现参赛者 AWD 主页面和核心组件。

**Files:**
- Create: `src/GZCTF/ClientApp/src/pages/games/[id]/Awd.tsx`
- Create: `src/GZCTF/ClientApp/src/components/awd/AwdRoundTimer.tsx`
- Create: `src/GZCTF/ClientApp/src/components/awd/AwdFlagSubmit.tsx`
- Create: `src/GZCTF/ClientApp/src/components/awd/AwdStatusMatrix.tsx`
- Create: `src/GZCTF/ClientApp/src/components/awd/AwdAttackLog.tsx`

- [ ] **Step 1: 创建 `AwdRoundTimer`**

`src/GZCTF/ClientApp/src/components/awd/AwdRoundTimer.tsx`：

```tsx
import { Card, Text, Progress } from '@mantine/core';
import { useEffect, useState } from 'react';

interface Props {
  roundStartTime: string;
  durationMinutes: number;
}

export const AwdRoundTimer: React.FC<Props> = ({ roundStartTime, durationMinutes }) => {
  const [remaining, setRemaining] = useState(0);

  useEffect(() => {
    const interval = setInterval(() => {
      const start = new Date(roundStartTime).getTime();
      const now = Date.now();
      const elapsed = (now - start) / 1000;
      const total = durationMinutes * 60;
      setRemaining(Math.max(0, total - elapsed));
    }, 1000);

    return () => clearInterval(interval);
  }, [roundStartTime, durationMinutes]);

  const total = durationMinutes * 60;
  const progress = ((total - remaining) / total) * 100;
  const mins = Math.floor(remaining / 60);
  const secs = Math.floor(remaining % 60);

  return (
    <Card withBorder>
      <Text fw={700} size="lg">Round Timer</Text>
      <Text size="xl" fw={900} ta="center" my="md">
        {mins.toString().padStart(2, '0')}:{secs.toString().padStart(2, '0')}
      </Text>
      <Progress value={progress} color="brand" size="lg" />
    </Card>
  );
};
```

- [ ] **Step 2: 创建 `AwdFlagSubmit`**

`src/GZCTF/ClientApp/src/components/awd/AwdFlagSubmit.tsx`：

```tsx
import { Button, Select, TextInput, Stack } from '@mantine/core';
import { useState } from 'react';
import { useSWRConfig } from 'swr';
import api from '@/utils/Api';

interface Props {
  gameId: number;
  services: { id: number; name: string }[];
  teams: { id: number; name: string }[];
}

export const AwdFlagSubmit: React.FC<Props> = ({ gameId, services, teams }) => {
  const [flag, setFlag] = useState('');
  const [serviceId, setServiceId] = useState<string | null>(null);
  const [targetTeamId, setTargetTeamId] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);

  const handleSubmit = async () => {
    if (!flag || !serviceId || !targetTeamId) return;
    setLoading(true);
    try {
      await api.game.awdSubmitFlag(gameId, {
        flag,
        serviceId: parseInt(serviceId),
        targetTeamId: parseInt(targetTeamId),
      });
      setFlag('');
    } finally {
      setLoading(false);
    }
  };

  return (
    <Stack>
      <TextInput
        label="Flag"
        placeholder="flag{...}"
        value={flag}
        onChange={(e) => setFlag(e.target.value)}
      />
      <Select
        label="Target Service"
        data={services.map((s) => ({ value: s.id.toString(), label: s.name }))}
        value={serviceId}
        onChange={setServiceId}
      />
      <Select
        label="Target Team"
        data={teams.map((t) => ({ value: t.id.toString(), label: t.name }))}
        value={targetTeamId}
        onChange={setTargetTeamId}
      />
      <Button onClick={handleSubmit} loading={loading} color="brand">
        Submit Flag
      </Button>
    </Stack>
  );
};
```

- [ ] **Step 3: 创建主页面 `Awd.tsx`**

`src/GZCTF/ClientApp/src/pages/games/[id]/Awd.tsx`：

```tsx
import { Stack, Grid, Title } from '@mantine/core';
import { useGame } from '@/utils/useGame';
import { AwdRoundTimer } from '@/components/awd/AwdRoundTimer';
import { AwdFlagSubmit } from '@/components/awd/AwdFlagSubmit';
import { AwdStatusMatrix } from '@/components/awd/AwdStatusMatrix';
import { AwdAttackLog } from '@/components/awd/AwdAttackLog';

export default function AwdPage() {
  const { game } = useGame();

  return (
    <Stack gap="lg">
      <Title order={2}>Attack / Defense</Title>
      <Grid>
        <Grid.Col span={4}>
          <AwdRoundTimer
            roundStartTime={game?.awdStatus?.roundStartTime ?? new Date().toISOString()}
            durationMinutes={game?.awdStatus?.roundDurationMinutes ?? 5}
          />
        </Grid.Col>
        <Grid.Col span={8}>
          <AwdFlagSubmit
            gameId={game?.id ?? 0}
            services={game?.awdServices ?? []}
            teams={game?.teams ?? []}
          />
        </Grid.Col>
      </Grid>
      <AwdStatusMatrix gameId={game?.id ?? 0} />
      <AwdAttackLog gameId={game?.id ?? 0} />
    </Stack>
  );
}
```

- [ ] **Step 4: Commit**

```bash
git add src/GZCTF/ClientApp/src/pages/games/[id]/Awd.tsx src/GZCTF/ClientApp/src/components/awd/*.tsx
git commit -m "feat(awd): add player AWD page and core components"
```

---

## Task 15: 前端管理员 AWD 控制面板

**目标：** 实现管理员 AWD 控制面板页面和组件。

**Files:**
- Create: `src/GZCTF/ClientApp/src/pages/admin/games/[id]/Awd.tsx`
- Create: `src/GZCTF/ClientApp/src/components/awd/admin/AwdGameControl.tsx`
- Create: `src/GZCTF/ClientApp/src/components/awd/admin/AwdServiceManager.tsx`
- Create: `src/GZCTF/ClientApp/src/components/awd/admin/AwdInstanceGrid.tsx`

- [ ] **Step 1: 创建 `AwdGameControl`**

`src/GZCTF/ClientApp/src/components/awd/admin/AwdGameControl.tsx`：

```tsx
import { Button, Group, Card, Text } from '@mantine/core';
import { mdiPlay, mdiPause, mdiSkipNext } from '@mdi/js';
import Icon from '@mdi/react';
import api from '@/utils/Api';

interface Props {
  gameId: number;
  status: string;
}

export const AwdGameControl: React.FC<Props> = ({ gameId, status }) => {
  return (
    <Card withBorder>
      <Text fw={700} size="lg" mb="md">Game Control</Text>
      <Group>
        <Button leftSection={<Icon path={mdiPlay} size={1} />} color="green" onClick={() => api.admin.startAwdGame(gameId)}>
          Start
        </Button>
        <Button leftSection={<Icon path={mdiPause} size={1} />} color="yellow" onClick={() => api.admin.stopAwdGame(gameId)}>
          Stop
        </Button>
        <Button leftSection={<Icon path={mdiSkipNext} size={1} />} color="blue" onClick={() => api.admin.nextAwdRound(gameId)}>
          Next Round
        </Button>
      </Group>
    </Card>
  );
};
```

- [ ] **Step 2: 创建管理员主页面**

`src/GZCTF/ClientApp/src/pages/admin/games/[id]/Awd.tsx`：

```tsx
import { Stack, Title } from '@mantine/core';
import { AwdGameControl } from '@/components/awd/admin/AwdGameControl';
import { AwdServiceManager } from '@/components/awd/admin/AwdServiceManager';
import { AwdInstanceGrid } from '@/components/awd/admin/AwdInstanceGrid';

export default function AdminAwdPage() {
  const gameId = 1; // 从路由获取

  return (
    <Stack gap="lg">
      <Title order={2}>AWD Management</Title>
      <AwdGameControl gameId={gameId} status="stopped" />
      <AwdServiceManager gameId={gameId} />
      <AwdInstanceGrid gameId={gameId} />
    </Stack>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add src/GZCTF/ClientApp/src/pages/admin/games/[id]/Awd.tsx src/GZCTF/ClientApp/src/components/awd/admin/*.tsx
git commit -m "feat(awd): add admin AWD control panel and components"
```

---

## Task 16: 排行榜 Mixed 模式显示

**目标：** 修改前端排行榜组件，在 Mixed 模式下同时显示 CTF 和 AWD 分数。

**Files:**
- Modify: `src/GZCTF/ClientApp/src/components/ScoreboardTable.tsx`
- Create: `src/GZCTF/ClientApp/src/components/awd/AwdScoreboard.tsx`

- [ ] **Step 1: 修改 `ScoreboardTable`**

在 `ScoreboardTable` 中，当 `game.gameType === GameType.Mixed` 时，增加 AWD 列：

```tsx
{game.gameType === GameType.Mixed && (
  <Table.Th>AWD</Table.Th>
)}

// 在数据行中
{game.gameType === GameType.Mixed && (
  <Table.Td>{item.awdScore ?? 0}</Table.Td>
)}
```

- [ ] **Step 2: Commit**

```bash
git add src/GZCTF/ClientApp/src/components/ScoreboardTable.tsx
git commit -m "feat(awd): add AWD column to scoreboard in Mixed mode"
```

---

## Task 17: 集成测试与验证

**目标：** 端到端测试 AWD 完整流程。

**Files:**
- 无新增文件

- [ ] **Step 1: 编译验证**

Run: `dotnet build src/GZCTF/GZCTF.csproj`
Expected: 成功，无错误

- [ ] **Step 2: 前端构建验证**

Run:
```bash
cd src/GZCTF/ClientApp
npm run build
```
Expected: 成功，无错误

- [ ] **Step 3: 功能测试清单**

手动测试以下场景：
1. 管理员创建 AWD 比赛（GameType = AWD）
2. 管理员添加 AWD 服务（上传镜像 + Checker 脚本）
3. 管理员启动比赛 → 确认容器创建成功
4. 选手访问 AWD Tab → 确认服务状态矩阵显示
5. 选手提交 Flag → 确认得分正确
6. 检查排行榜 → 确认 AWD 得分显示
7. 测试 Mixed 模式 → 确认 CTF 和 AWD 分数同时显示
8. 管理员停止比赛 → 确认容器清理

- [ ] **Step 4: Commit**

```bash
git commit -m "test(awd): integration tests passed, AWD mode fully functional" --allow-empty
```

---

## 执行选项

**Plan complete and saved to `docs/superpowers/plans/2026-06-04-awd-mode.md`.**

**Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach would you prefer?**
